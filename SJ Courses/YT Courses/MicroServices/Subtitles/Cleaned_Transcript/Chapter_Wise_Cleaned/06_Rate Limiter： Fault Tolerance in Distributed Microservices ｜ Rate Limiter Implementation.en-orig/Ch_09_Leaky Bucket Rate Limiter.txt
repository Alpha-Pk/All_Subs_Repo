=== Leaky Bucket Rate Limiter (2039s - 2156s) ===

Okay, another is leaky bucket. Leaky bucket is considered as like requests are cued and accepted at fixed rate. If Q is full, incoming request will overflow. So let's say you have an incoming request. Okay, bucket or you can say that a Q which is keep on accumulating the request. Okay, and the Q's are accepted at a very constant or very fixed rate. So consider this with the flow diagram. A request comes if Q is full. If this Q is full then it will send denied and it will send HTTP 429. Too many requests can't even accept. If Q has some space then it will add the request at the end of the que and from the Q our system will keep accept the request in a very constant rate very very constant rate. So very simple but disadvantage is your latency will increase because request might have to wait in Q for its turn. So request which is present here it has to wait for the previous request to finish it and to be accepted and your queue is become a bottleneck. So side size should be set properly. If the size of the queue is very small you would be denying lot many requests. If the size is too big your latency will increase a lot. Okay. And also memory you need it. Right. So these are various algorithms of rate limiter. I hope with this examples it will be clear. Just go through this example one more time slowly and read it those text like uh the definition which I have written like this right you read it you will get clarified little better. Okay. So we will now let's see that how it got implemented in spring boot.
