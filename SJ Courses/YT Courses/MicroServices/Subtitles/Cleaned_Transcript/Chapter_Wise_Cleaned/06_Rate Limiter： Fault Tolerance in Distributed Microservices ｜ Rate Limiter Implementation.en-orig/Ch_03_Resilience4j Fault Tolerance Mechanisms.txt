=== Resilience4j Fault Tolerance Mechanisms (268s - 397s) ===

framework. So resilience 4j is a combination of mechanism like rate limiter, bulkhead, time limiter, circuit breaker and retry. So these fives are the combinations or you can say that the mechanism which resilience 4j provides which helps to develop a micros service as fall tolerant. And one more thing I would say that we will go one by one in a recommended logical ordering. You might say what is the ordering issue like we can we can apply anything in in whichever order but see this is the logical order rate limiter first then bulkhead then time limiter then circuit breaker and then retract right why because there is a reason like let's say that if you are applying right for example rate limiter should always be applied before retry now let's say you are applying first retry and then rate limiter. This mechanism you are applying in your system like this. So does that make sense to you? Just think what if rate limiter is that it whenever an access traffic comes right it blocks some of the traffic. Now if you do the retry mechanism first means you are unnecessarily wasting your computation in retrying on those traffic that ultimately will get blocked by the rate limiter. Okay. So there is no point of retrying those traffic which ultimately get blocked. So that's why rate limiter should be first and retire should be second in this cell. So that's why if when you are applying all the five mechanism in your system logical ordering should be present that's what there is no hard and fast rule but this is you can say that uh de facto which generally should be followed like this should be the order okay so let's start with our first
