=== What is Rate Limiter and its Types (397s - 470s) ===

mechanism which is rate limiter very very important from interview perspective even when you are building the services also in product based system like that's also is very important. So rate limiter is what it controls the number of requests allowed to a micros service in a given time window. Let's say that in this time window a particular time window limited time window let's say 5 second 10 second whatever how much request can go through. So in short it protect our system from sudden traffic spike like a DOS attack. Okay. So there are multiple mechanism or algorithms in rate limiter. Before even we goes to the implementation. Let's quickly see what are all the algorithm. So there are most commonly five algorithm fixed window counter sliding log sliding window counter which again has two flavors token bucket and leaky bucket. So quickly in 5 10 minutes we will try to go through each so that uh when we see the implementation we will at least get to know what rate limiter actually does internally.
