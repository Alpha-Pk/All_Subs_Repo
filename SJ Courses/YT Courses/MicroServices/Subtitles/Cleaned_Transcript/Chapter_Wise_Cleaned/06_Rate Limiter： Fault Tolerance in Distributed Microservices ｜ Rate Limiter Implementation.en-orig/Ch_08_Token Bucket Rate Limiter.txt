=== Token Bucket Rate Limiter (1898s - 2039s) ===

Then we have token bucket. Token bucket is very very simplified versions and uh what happen is we have a kind assume it like we have a bucket and we placed a limited tokens inside it. Each request consume one token. If there is no token left request is denied and there is also a refiller which adds the token in bucket at regular time interval. If bucket capacity is full and cannot hold more token, token overflow will happen and token will be rejected. So understand with this is that this is one bucket which has capacity of four token. So 1 2 3 4 four tokens are present. There is a refiller which task is to add one token every 6 seconds. Okay. So now let's say that at 1 2 3 4 5 second five request comes. So first four because we have four tokens, right? So first request take one token accepted accepted. Accepted accepted. Now all four tokens are used. Okay. Now fifth request come any token left. No. So it is rejected. Now at sixth second a refiller adds one token. So refiller added one token into the bucket because we have configured it that add one token every 6 seconds. So at 6 second one token is added. Now if any request comes it will use it and accept it. Very very simple. So disadvantage is that a quick burst is possible if token capacity is not set properly. What if I have put a token that in a bucket I have capacity equals to 1,000. I without doing proper computation I I give random capacity. Then means that much token can get accumulated and what if in a one second it's a sudden request comes all thousand so all thousand will get accepted so there is a chance of bust traffic okay because whatever the amount of tokens present that much concurrent request can be accepted so you have to consider this token capacity properly
