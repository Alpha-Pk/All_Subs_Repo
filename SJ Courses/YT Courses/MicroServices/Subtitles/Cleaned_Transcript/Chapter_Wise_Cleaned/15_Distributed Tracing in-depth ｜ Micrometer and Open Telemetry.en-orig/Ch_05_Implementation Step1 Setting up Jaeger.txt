=== Implementation Step1: Setting up Jaeger (1918s - 2044s) ===

it and uh otherwise you can even um install it and run it. Okay. And uh like it's a you can search over the internet. It's a very easy step to install and run it. So I am just running this command. I have a docker installed in my system. So I am just running this command and this Jagger got its image got installed here in the docker. So what this command is doing it's checking if image already exists locally otherwise it pulls the image from the docker hub creates a container run it in the background okay and these are the export it exposed one is 16686 this is for UI okay 4317 this is gRPC endpoint if you're using that one and 4318 this is our HTTP endpoint HTTP OTLP endpoint. OTLP HTTP endpoint. This is what actually we are interested in because our OTLP exporter need to dump the span which are in which format? OTLP format. Right? So we need a port number the URL and port number where we can push it. So we are running it local. So it's a local host and port number is this one 4318. Okay. And the UI is 16686. So if you open this after running this you will see that Jerger UI is up. So here if you see this is my docker I have a container Jerger. So I'm just running it starting it up. Now here if I refresh it currently service is zero and Jerger UI is up. So first step is done. That's the only first step I do. I just I have a docker. You can install Docker also. Very easy. And you have to run this command and Jagger is like installed in our system up and running. Now let's come to our application. Now
