=== Demo of Manual Span Creation (4051s - 4202s) ===

the trace application one API 1 controller. I have this SL API/ test SLS span the same thing whatever I have told you take out the parent span create a child spend from the parent give it a name started the timer update the current span and just put thread slip in the finally closing this span so that uh the current span would properly reset and after that I'm stopping the timer. Okay. So, let's hit it. / API slash this one. Let's hit that response from F1. So, that's what it is returning response from F1. Let's see the Jagger. So, here it goes now. So, here if you see if I click here, so it created two span. So this is for the parent one and here if you see manual child span this is what we created right manual child span and if you see it's JSON so this is the JSON information so spend one span two this is an array trace ID is the same for all so this is when the incoming HTTP request for / API/ / test / span no child because this is the first call no child no parent this is the time it started this much time it took when it might have gone so this much time it took now this is the span two this is the same trace ID different span and this is like manual child span so that we are creating a new span within this okay so it's a child of this span. So it's a child of this span. So this is the parent span. Internally we are creating a new span. So this is a child. So we have a proper information. So parent span is this one and this time it child spend start uh start started when it finished. So overall duration it took. Got it? So simple right? So practice
