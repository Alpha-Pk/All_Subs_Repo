=== Centralized Configuration Architecture (276s - 396s) ===

Solution for is centralized configuration with spring cloud config. Very important. you will find mostly companies are using this. Okay. So here the first you can say that this is the our layer one in the first layer which is we have our config files. So we create a g repository just to store the config files. Okay. This is where we put our dotproperties files. Okay. Even though you have multiple microservices for all the microservices we we have to put the configuration files property files here in this git repository. Okay. So this is our first thing we will see we will go in depth of each of layer uh separately. So this is the first thing. Now the second thing is another micros service config server. So its task is to fetch the config from the g. So here so this config server fetches the application.properties from this g repository where we have only the property files. Okay. And in the layer three you can say that we have our microservices individual microservices it connect with the config server. Okay. So you can say that order is connecting with config server and saying that hey can you help me fetch the properties for this order application. It goes to the config files get repository where the properties files are there. It fetches the property from there and provided to the order service. Okay. So now let's go one by one at each service.
