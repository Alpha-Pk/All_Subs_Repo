=== Filters and their types in API Gateway (Global and Route Specific) (22s - 126s) ===

need to understand filters in API gateway very important so filters can intercept and modify the HTTP request and response as they passes through the gateway. Okay. So there are two types of filter. One is global filter. Another is root specific filter. Sometime uh engineers also call it gateway filters. So what are global filters? As the name says apply to every request that goes through gateway. Every. Okay. And very good example could be of like performing authentication. You want to do logging. So those are some of the very good use cases for the global filters. And root specific filters are like as the name says again apply to specific requests that matches a particular route. Okay, we will not apply them globally to all the traffic. No only particular traffic which matches a particular route. Okay. And very good example would be you can say or you use case would be rate limiting circuit breaker resilience retry that if you have multiple microservices you want different different configuration for for this circuit breaker configuration could be different for this circuit breaker configuration could be different okay I don't want to apply globally to all my to all the roots okay so we will see that but for now just have this definition like uh basic understanding and later on it will get more clarified.
