=== Placeholder in Log Statement (1264s - 1407s) ===

Then we have something called placeholder in log statement itself. So till now what we are doing is log.info and it is printing it. There might be a many case where we have to append something like placeholder like here what I'm doing is like this log.info user who is the user shange created with ID. What is the id this? So I am appending it. So this is not recommended in logging framework. Okay. Never do string concatenation. Why? See every log statement has level info log.info. So you don't know whether your logger will accept this statement or not because see your logger has let's say level one. So it definitely it will not accept it. But since you are doing a screen uh string concatenation no matter whether your log statement get accepted or not this will get build up. Okay. So it's always better or recommended to use like this way log.info user and then give a placeholder created with id placeholder and after that you can give like so this will go here this will go here. Now this placeholder would only be filled up when your log statement is accepted. Okay. So you don't unnecessary has to create the string even though it is getting rejected. Okay. So you will save some CPU cycle in a heavy traffic taking API each CPU cycle matters. Okay. And in case of exception so let's say log error payment failed for user one placeholder. So this shriash will go over here and then comma exception. So exception would always be last. Don't put e comma like shriash. No. So whenever any exception happens if you put it at last stack trace would get also printed. Okay. So always remember is that if you have to add exception into log dot error it would always be last. After all your placeholders got completed, put it at the last. So these are small things. Now let's see
