=== Log Format / Log Pattern (1153s - 1264s) ===

told you right we have something called encoder okay we have encoder they decide the format in what in what format ultimately output would be shown it would be shown either console or file or what this is just an place where the log will go but in what format it will go that would be decided by our encoder So like here this it is console. So on console this is the way it will go date level then your logger name message. Okay. So if you open this pattern layout on Java. Okay. So we are using pattern right. So if you open pattern layout on Java this is a logback framework class. You will see so many default which we can use. So here you might be thinking right how would I know what to use what is D means what is level means what is logger means. So here in this class you would see that D it is calling this date converter. Similarly level it is calling level converter. So if you open this class level converter you will see what it is doing is from the event log event it is take out get level. Okay. So all this thing we can use as a placeholder in our pattern. very simple. Okay. So just open this class. It's there are so many so it's not possible to remember it. Whenever you find a use case that okay this value I need to use open this class see if it is present and that placeholder you can use it here. And if you want to go into the implementation part what it will do just go open the class and see what it is doing it. Okay. So this is how generally log format log pattern works.
