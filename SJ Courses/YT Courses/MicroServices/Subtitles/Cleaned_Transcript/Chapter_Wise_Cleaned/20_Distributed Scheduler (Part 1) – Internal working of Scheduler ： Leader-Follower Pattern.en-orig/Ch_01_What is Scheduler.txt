=== What is Scheduler (0s - 126s) ===

back and today we are going to do distributed shoulduling. Okay. So we will not just cover watering we will also go one level deep like how it works internally. So it's going to be very interesting. So let's not waste more time and start it with like what is scheduuler. Okay. So in very simple term if you have to describe we can say scheduleuler is nothing but a background thread that sleeps until a given time. Wake up perform a task and sleep again. Okay. So this is the most simplistic definition we can say about scheduleuler and spring boot does not perform any magic. Okay. Internally it just make use of scheduled threadpool executor. So if you remember from the Java series in Java series we have covered thread pool and threadpool executor and also scheduled threadpool exeutor. So spring boot internally make use of scheduled threadpool exeutor itself. Okay. So we have to understand this very like how actually this works. If we understand this spring boot in spring boot how we performuling how distributeduling works that is like like it's like uh wrapper around this uh schedule thread pool executor okay so I would say that the prerequisite is that you know what is threadpool and what is threadpull executor schedule threadpool executor I will go one again here in more depth And uh but at least this is I would say that it's a prerequisite like what is thread pool executor when we create a thread pool executor object what happens like it create a queue it create a pool of worker threads how the task is picked and so at least basic we need to know what's the flow of the thread pool and thread pool executor okay so with that said I would start
