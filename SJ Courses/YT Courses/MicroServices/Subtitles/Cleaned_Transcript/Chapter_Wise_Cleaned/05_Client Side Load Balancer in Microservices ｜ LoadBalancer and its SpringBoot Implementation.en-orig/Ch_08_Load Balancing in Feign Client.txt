=== Load Balancing in Feign Client (2355s - 2505s) ===

template you can even the f client rest client is using the same load balancing interceptor only no change at all. So if you recall from the service discovery video right if you recall for the fing client service discovery I told you that we have to add a load balancer also because internally fing framework does the same thing okay because it once you get a multiple instances it has to pick one instance right so it use a load balancer okay so for fink client we don't have to do anything whatever the service discovery we have to do it's the same thing right now see this one like I I'm hoping that you have seen the service discovery video and you can correlate this like without service discovery in the f client we have to provide the URL but with service discovery we have to only give the name hey I want only product service that's all okay and we just invoke it. Now let's say that if you want to change the algorithm you don't want faint client to use by default you know that internally if it's using a load balancer it might be using round robin you want that hey I don't want round robin I want some different algorithm can you even if you are using faint client can you change it now easily same in the application if it is only one client then you can use load balancer client provide a name service this for this service this configuration have to be use I have written this configuration which will get also get invoked at runtime when I'm going to invoke this service get product by ID. So here I just changed the load balancing algorithm new load balancer okay and attach it with this service ID. Okay. Now if you go inside this load balancer choose method this choose method for that one that internally it will do what we did first call service discovery and then it will randomly pick one of the time the same thing. Okay. So even though everything is
