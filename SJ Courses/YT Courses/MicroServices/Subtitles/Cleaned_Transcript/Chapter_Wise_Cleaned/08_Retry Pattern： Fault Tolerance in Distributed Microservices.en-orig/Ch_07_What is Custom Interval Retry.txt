=== What is Custom Interval Retry? (1174s - 1217s) ===

And the last type of retry is custom interval like you define your own retry logic. Hey, I want retry to be like 1 1 2 3 5 like this. Okay. So first retry should wait for 1 sec 1 millisecond then another 1 millisecond then 2 millcond then 3 millcond then five then 8 like this. So you can define your own custom logic how you want to do a retry that is also possible. So we have full control and flexibility but disadvantage is you have to maintain you have to maintain it. So you have to write the logic you have to maintain it. The framework will not help you in this. We will see that. So first let's see the implementation of
