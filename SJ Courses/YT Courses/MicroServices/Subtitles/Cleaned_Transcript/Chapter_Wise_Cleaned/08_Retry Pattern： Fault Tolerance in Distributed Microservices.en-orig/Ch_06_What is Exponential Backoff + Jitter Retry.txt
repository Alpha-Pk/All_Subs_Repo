=== What is Exponential Backoff + Jitter Retry? (1027s - 1174s) ===

So there is one another version of it is like exponential back off plus jitter. Jitter means some randomness similar to exponential backoff but add jitterness or randomness to delay or to you can say that to avoid retry storms. Now see exponential backoff already uh increases the delay time exponentially right but only thing which missing is randomness. So this jitterness helps to retry uh reduce the retry storms. So this is its formula. We have to choose a random number between random between 0 comma minimum of maximum delay. What what is the maximum delay we can have? Let's say no exponential back off. So it's not like let's say you have for example I'm just saying you have 100 retry so it's not like it continuously increases the time and all no there should always be limit right after a limit this is the maximum wait time let's say uh 5 second this is the maximum delay time more than that you can't go increase if you more than increase if your for with the formula if it gives you the value of more than 5 second always use 5 seconds so this is the maximum limit. So here is the same random you have to choose a random between 0 comma minimum of the maximum delay or this is our exponential backoff formula. Okay. So now we have bring some randomness. So again random between 0 comma minimum of maximum delay which we can wait comma your exponential backoff formula one. So between these it now pick some delay value and now since this is random you can say that uh it spread the retries over time reduce peak load and thundering her issue is pro happening of this issue become very less because of this randomness. The disadvantage is it's not easy to debug and it's little complex.
