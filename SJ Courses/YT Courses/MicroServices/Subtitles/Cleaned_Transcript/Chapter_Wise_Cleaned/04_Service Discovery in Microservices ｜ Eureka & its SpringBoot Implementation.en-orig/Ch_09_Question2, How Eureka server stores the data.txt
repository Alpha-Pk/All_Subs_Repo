=== Question2, How Eureka server stores the data (1786s - 1867s) ===

stores the data? Ureka server only store data in memory. There is no uh you can say that DB persistence. No, only in memory. It creates a map string, lease uh instance info. Okay. So here if you see that the key is your app name/ instance ID. So this is generally your key. what is the app name and what is the particular instance ID IP port number and the particular service. Okay, this totally generate a unique instance unique instance because IP is different port number IP port number and the product app name. This makes a unique instance. So this is your key and in the value you have the value of like app name, IP address, host name, port, whether this is up or down, when it was last renewed, lease duration, etc. Okay, so this is how Ureka server stores the data. But there is remember there is no DB persistence. It's all in memory.
