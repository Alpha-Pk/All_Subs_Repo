=== One Time Scheduling use in application (424s - 503s) ===

that is also equivalent to schedule method in the part one which we have covered. Okay. So here means we have to run the job only one time only one time. So this is my task I put at the schedule. Now initial delay so when you need to start so they're the same thing the task and the delay. Okay. So here let's say if you want to start it immediately just put zero. If you want to start let's say after 2 seconds just put here. So all this is in milliseconds. Okay. But here initial delay is mandatory. You can't have you if you don't define it here you will get an exception. So you should define initial delay in case of one timeuling. Okay. But if you are using fixed delay or fixed rate if you don't add initial delay by default it will take zero. Okay. But only in case of one time shoulduling you have to specifically add the initial delay. So in this case what would happen is after an initial delay of 2 second job will start and it will get executed only one time.
