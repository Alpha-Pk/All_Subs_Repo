=== Introduction and pre-requisite (0s - 42s) ===

Welcome back and today we going to cover distributeduling part two. So if you have not seen the part one where we have covered like what is scheduuler, how it internally works, we have also seen how threadpull executor works internally. So we have already seen in depth of it all its method like schedule method which runs only one schedule at schedule at fixed rate with example schedule with fixed delay with a proper example and also the internal functionality. So that's the prerequisite for this video. Okay. So a spring boot does not
