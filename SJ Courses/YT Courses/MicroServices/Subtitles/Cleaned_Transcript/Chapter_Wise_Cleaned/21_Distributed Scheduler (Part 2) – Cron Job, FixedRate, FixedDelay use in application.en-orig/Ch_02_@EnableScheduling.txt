=== @EnableScheduling (42s - 138s) ===

perform any magic. it internally make use of schedule threadpool executor itself. Okay. So in other words, it's just a wrapper above schedule threadpool executor. So let's see that how to configure theuling in spring boot. So the first step is enable theuling. Okay. So in your spring boot application you have to add this add the enablesuling annotation because after this only all the scheduleuler related beans will get loaded. Okay. And after this only it will be have the capability to scan for at the rate scheduled annotations. Without this theuling will not even work. It doesn't even know. Okay, it won't even scan the scheduled annotation. So that is a very basic step. After that we just need to see what type of shoulduling we want whe the fixed rate, fixed delay, one-time shoulduling and chron job. Now if you have already seen the part one we have already know in depth of how this three works. We have already have like schedule method schedule with fixed delay method and schedule at fixed rate method. So internally all these types in spring boot actually use what we have already seen in uh schedule threadpool executor methods. Okay. So only thing here new is like chron job. So let's see that the fixed rate. So how
