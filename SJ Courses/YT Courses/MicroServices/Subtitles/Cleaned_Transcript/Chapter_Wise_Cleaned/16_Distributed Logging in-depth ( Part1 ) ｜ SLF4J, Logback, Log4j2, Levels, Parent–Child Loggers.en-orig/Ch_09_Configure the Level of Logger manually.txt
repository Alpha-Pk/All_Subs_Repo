=== Configure the Level of Logger manually (1616s - 1682s) ===

So we can change if you let's say if you want to change the uh level from info to let's say something else for this logger we can change it through the configuration. So in application.properties we can change logging dole and now the name name of the logger. Okay. Logging dot level and then after that the name of the logger whatever the level you want debug one info capital letter like this you can give. Now if I start the application and hit this endpoint so I have a debug. So I will only enterain equal or higher. So error would be entertained warning info debug. But trace is lower priority than debug. So that would be skip error warning info debug but trace is skipped.
