=== What is Logging (14s - 105s) ===

So let's start without wasting any time. What is logging? It means recording important application events like request, response, error, warning or any information. Okay. So that we developers can monitor and debug what the application is actually doing at runtime. So for example, you own the user component. Okay. And you have one endpoint / post which task is to create a user. So as part of logging you can write any information into the logs. So let's say I am writing one event that user created okay something and some additional information. Now let's say in future if a user comes that he or she is facing a problem then you can check the particular log and see okay I see this printed into the logs mean till here my code is working fine. So you see that a log can help you in your debugging and it is not like you have to only log request or error or something very very critical even some information which can help you in the debugging that also we can log or we can record. Okay. So before we go with distributed
