=== Logger Name and its use (1247s - 1347s) ===

And then we have logger object. So these are the logger objects. So logger objects have few important fields present within logger class. So one is name, level, parent and child. Okay, one parent, multiple childs. Now let's see this because these are the crux of the logging framework. Name. So this is the name of the logger. It can be anything I told you. It can be package name, it can be class name or any string. So for a given logger name only one logger object is created and reused. We have already seen that. But in real world application the common practice is to create one logger per class. That's why in production you always see that we are doing payment controller dot class the class name and then we put dot class because it will use the name as a class name. So when you are using like this let's say my payment controller is my class name. So when I use like this payment controller dot class so the name is that com.concepts dot payment controller the full class path. Okay. So this would be my name of the logger and this is generally in the production we say that one logger per class otherwise you can give any name. So it's like one name one logger object. So name is clear. Then we have level
