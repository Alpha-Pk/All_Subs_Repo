=== Semaphore Bulkhead Use-Case discussion (161s - 258s) ===

one is very very simple use case. Let's say you have order. This is your component and this is your downstream product. So order invokes product API. Let's say product is a small and lightweight service and can only handle three concurrent requests. Okay. And product itself is like very lightweight. It doesn't have put rate limiter on itself. Okay. But with the bulk ahead you can control that. Okay. So you know that product can handle only three concurrent request at a time. Okay. So it accept three requests all at the same time. But when the fourth comes no there is no thread left. So when any one of them get free then the another request would get fulfilled. Okay. So this is very very simple use case very simple use case and this use case is resolved through simma 4 bulkhead. So there are two types of bulkhead. One is simma 4 bulkhead we have seen another one I'm going to tell but for this use case whenever interviewer put this use case simma for bulkhead is the answer. Okay we will see it how it functionality but I think this is clear right? Whenever there is a condition that uh you have a downstream which can only accept this many concurrent requests how you can handle it through simma for bulkhead.
