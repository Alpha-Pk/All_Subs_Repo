=== RefreshScope and how it resolves the problem (409s - 519s) ===

solve this. So what refresh scope does? So this annotation help to mark the bean which need to be destroyed and recreated or you can say that which need to be dynamically refreshed. So when we are saying that hey it's refreshed actually it is doing two things. it is first destroy the bean and it create a new bean okay with the latest value. So now see this example in the order properties right where I have the configuration. So this is my actually the property custom dot message. This is the property right custom dot message. So custom dot message will get ultimately mapped to this field. Okay. So now and this is a bean at the rate component. So now I have marked at the rate refresh scope here. So when I use add the rate refresh scope over this bean I am telling a spring boot that this bean add uh order properties say in this scenario I am allowing it to dynamically refresh. Okay. And dynamically refresh means whenever the refresh happens, it will destroy the bean first, old bean first and it will recreate a new one with the latest value. Okay, and how that refresh will happen. Okay, it will happen one way is through actuator.
