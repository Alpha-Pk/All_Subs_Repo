=== HATEOAS Introduction (0s - 362s) ===

hey guys welcome to concept and coding this is Shan and today in Spring boot I will be covering hatos restful apis yeah weird name right and even if you see its full form like its full form says that hyper media as the engine of application State Mo here right so before we start if you search over the internet and you will see that hey is it still popular you will see many feedback which says that nowadays companies are not using it it has so many disadvantage don't use it but I would say that hey guys welcome to reality hatos is being used in very big product companies today also right and I will not only tell you what how not to use it but also tell how to use it what advantage it adds it tells the client what the next action you can perform after a particular step let understand this with this example so here in this simple example let's say that I have reached to this step using a post API SL add user I am able to add a user but with currently it's unverified now what's the next step I can do the next step I after adding a user I can do is I can make a get call and get the details of this user I can even delete this that also I can do or I can even verify this user right so verify again has different ways so you can say that after verifying uh there are different ways to verify one is through SMS so when I say that SMS verify start let's say that you get some PIN code and which you might have to enter through which you will finish it so different API for start different API for finish similarly for email verify start where you will get a email has been sent to user that hey this is the code now you have to push it somewhere so you will actually finishing your email verify so this is a small use case which I have come up with and I will tell you why it is important to understand when we will see when not to use and when to use it but you got it right when I say that it tells the client what's the next action you can perform after particular step so this is one step and what is the next action after adding a user either you can do a get call either you can delete it or either you can verify it these are the next action you can do okay and we will see uh more details but let's see that how the response looks like so now let's say that I am using a post API to add a user now the response of it if you are not using a heos link how it going to look like so this is the response you will get when you add a particular user you got a user ID okay this ID is representing a unique ID in your DB through which you can fet the details so we return a user ID and let's say name and currently what is the verified status let's say it's unverified but what if I want to use heos Link heos restful API so in this case with heos link this is how the response generally looks like we send the user ID same name verify status unverified but see this is so links inside links we send what all the next step you can do so it's an array so it's an array you can even have more like this so all the actions which can be done after this current state what all the next things you can do you can put it into the links so here I have just for example I have added one link where it says that relation is self the URI and what is the type so this is nothing but a HTTP method for this URI whether it's a get call Post call patch call delete call so we can add that URI is the what is the next URI can be invoked right and what is the relation of this API with this current state okay so currently I put self means this API will return you the details of this only it's generally a get call self is always def facto you can say that even if I don't write get right and if it is relation is self it's a def facto it's like a get call for this one if you have to F the details of this user ID generally self represent that only that it's a get call okay but still we will see more details about it but for now what I wanted to show you without hatos link generally what how the response look like and with hatos how the response generally looks like so this is what actually important here we provide what all the next action can be taken after this step how to do that I will tell you but before understanding how to do this let's understand when to use and why to use hatos Lang very important right so the are two major purpose of
