=== Request Bean Scope (1043s - 1598s) ===

initialized third is request this is where ultimately confusion comes in because that's where one more things comes into the picture when I show you this part uh this proxy mode so this proxy mode also comes into the picture here and that's what make uh engineers get confused with this request request is scope so what does it mean new object is created for each HTTP request for each HTTP request let's say fetch user this is one HTTP request Now new object will be created for all the classes which are marked as request right and if in this API if a particular object is required four times we will not create four objects only one time okay let's see that with an example and again it is laily initialized so example is very simple again I have created a test controller class right so it has one this API and I have marked it as request this has two dependency one is on user one is on student so let's see uh user first so user also I have marked as request right so means for each HTTP request a new object has to be created no dependency on this one and for a student I called it as a prototype means every time no matter what every time new object should be there and it has a dependency on user so now let's see that when you start an application what would happen and when you invoke this HTTP API what would happen how this uh dependency resolution happen and the Scopes comes into the picture so when you start an application ioc will look for all the component at the rate rest controller and try to create a bean so this it would find test controller one scope is request so this is lazy initialized nothing would happen then it next goes to student it found this at theate component scope is prototype again a lazily initialized nothing will happen then it will look able to see user because it is marked as atate component but again its scope is also request which is lazily initialized so no object is created at the application startup so at application startup here you see that started spring boot application but no object is created okay makes sense now I am hitting the API fetch user the first time HTTP request is coming now HTTP request is coming now what would happen so it has to create an object so this is request right so I called Fetch user so now with for this HTTP request first test controller object would be created test controller one test controller one instance initialization okay this will get printed then second it will look to resolve the dependency so it will goes to the user user first user is also marked as request so means only one instance or new instance should be created for each request so for this request is there any user request now no so it will create new object user initialization so user initialization would happen any dependency no so post construct would be user object hash code so some hash code would get come here okay so user will get injected here after that it will resolve a student student is prototype no matter whether this same request different request it new object has to be created so student student instance initialization now it will go and resolve the dependency it say that user now here if you see that user is marked as request one object one object per request so one object is already created so student will use this no new object would be created so student will use this user right and student hash code so his hash code will come and what would be the user it it going to use this user hash code so like this so this is the will uh expectation so test controller initialization happen so test controller ultimately goes to our user right so we have seen that we have seen that user user object created with this one and this got injected into this test controller now then student so student is prototype so new instance would be created and student is dependent on user and user is marked as request right so same object would be used right the same thing is happening which we have seen with diagram so you got it right and this ultimately the test controller has this user and this student object okay so this is if this is clear if this is clear the same thing is uh actually printing in the logs actually now what would happen if the second time you are hitting the API when the second time you are hitting the API what would happen so till here this is first call right this is first call now from here second call happening this now when the second call happening again I have also copy pasted the same diagram here so that I don't have to scroll much so this is request right so now second request is coming so means a new object would be created for this one so it goes to again Auto user user is again request a new object would be created for this one student student is anywh prototype doesn't matter right it will use it will create a new object itself so I'll show you that first test controller instance initialization so now here if you see when I when I hit the second time it is trying to create a new Bean for the test controller one then it goes to the user and it create a new object for the user earlier was this now it's a different object because user is marked as request and this is a new HTTP request then it goes to the student student is prototype so no matter same request whatever new object would be created right and then test controller in the test controller we it has an object and student right so now here if you see it has this user and student also has the same user so in the one request one HTTP request no duplicate objects is created only one time so test controller class student class both wanted user object so user is created only one time 8929 right the same 8929 so both test controller and student is using the same user class okay and each HTTP request a new object is created I hope it's clear what does it HTTP uh scope request means now the problem comes in can you answer me this one consider this below
