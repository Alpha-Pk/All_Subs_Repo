=== N+1 Problem (1986s - 2589s) ===

there is one when we talk about one to many and many to many there is one issue comes is n+ one problem it is very frequently Asked in an interview questions also okay I I'll tell you it's not that hard but yeah there is something like this itself so see the query wise whether it's one to one or one to many the query wise there is no change so now here if you see that I'm creating one too many user detail can have multiple address one user can have many addresses so I just changed one to one to one to many we already seen that how to do that it's very simple right and the queries also Remains the Same okay select all the fields of a user detail I am putting a join the join is with user detail list ud. user detail list right there there is no need required of on it already knows that where the name is this okay so I'm trying I'm searching my I'm doing a search on the user detail where the name is whatever that's coming into this method and it will ultimately do a join at fetch all the list of user addresses also okay now here if you see that it's this query can result into n+ one problem how the query wise it's exactly the same first see what happen if you remember we learned something about eager initialization and lazy initialization okay with now let's say that you have a parent and you have multiple Childs child one child 2 child three with lazy it first fetches the parent only and when we are trying to access its child then only it will try to find all its child and in the eager what we have seen till now is when you have a multiple Childs and you are using eager it will at the first when you're trying to find a parent itself it will automatically also load the child itself but remember one use case where eager does not work in this manner let's say that you have a query so this is multiple parents this is multiple each parent has multiple Childs each each parent has multiple Childs if your query is query is like is fetching one parent and multiple child eager will work like this what we have seen let's say it will fetch this and all its child also but if your query is like it can fetch multiple par and also multiple child okay if your query is such that it can fetch multiple parent right and for each parent multiple child also then your eager does not work like what we have seen okay so how it works is that let's say I'll tell you in a simple way you have two user two parent parent one parent two okay and and it has three child this also has three child and we have created a query that which is matching both the parents okay so what how the query will run like this first query it will fetch all the parents okay so here with this we'll get parent one and parent 2 okay now it will create two different query one for parent one to fetch its Childs and another query is for parent two to fetches all its Childs okay so just now imagine instead of this two just imagine there are n and each parent have any number of child so if your query is like it can fetch n number of parents so one query it required it will fetch n number of parent parent 1 parent 2 parent n okay one query is to fetch n number of parents then n number of time it will run for each parent and it will fetch all its Childs parent one Childs parent two Childs like this parent and Childs so one query plus n queries the total n+ one query it will hit to fetch all the data you got it like the only the scenario when you have multiple your query can fetch multiple parent and each parent have multiple child then because of performance issue the jpa does like this okay it eagerly doesn't work like okay for each parent it automatically goes and fetch all the child in the start itself no okay first it will only fetch all the parents and then let's say that you are doing get child for each parent it will create it will hit the N number of queries to for each parent it will get the child so this this the same thing I have explained here now say I have one user can have many address and our query is such that it can fetch more than one user more than one user mean more than one parent it can fetch then only this can problem can occur so say we have n users then below query hit by jpa is one query to fetch all the users okay so one query you will fetch user one user two user n right and each user can have multiple Childs so for each user now it will hit and fetch it Childs user to child so similarly n times it will fetch so total n query so total n+ one hit it will do so we need to find a way so that one query only hit instead of n+ one or any better performance than this before going to the solution for this problem one thing might be coming to a point is why not eager initialization I've already explained you before that eager initialization only works when you are like only one your query is fetching only one parent and even though it have multiple child doesn't matter it will go even if you're trying to fetch one parent it will automatically go ahead and create a query such that it will also fetch its child also and if you remember we all did a testing by find by ID so means only one parent we are only getting but as soon as you made a query such that multiple parents are possible eager initialization won't work because of performance issue okay so here you can uh see this an example I have very simple use case I have created two username both AA okay okay and they have uh this also user ID one also has certain child user ID to also has certain child I have kept only one one they can have multiple child at now see my query how it is that select all the roles from the user detail join the user address list where name equals to user first name okay and the in the what I have given in the query AA so now my query can user details you can see that multiple parent rows can come for each parent rows there can be multiple children's row so how this works is that it it fetch me the data but how the queries it will hurt is the first query it didn't do eager initialization right so here it first fetches only the user detail all the user it fetch all the user detail itself see user ID right all the user ID details like what we have commanded over here now for each user it is hitting so I have two users right so for this id1 it is fetching its child for ID user so this is for user one this is for user two it is again fetching it child so now here if you see address details okay so now for each user it is making a select query so two queries it is making so Total 1 + 2 total three queries Okay so that's why it is known as 1 plus n issue so is like n number of parents I have so that many plus one to fetch all the
