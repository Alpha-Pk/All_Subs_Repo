=== Derived Query (65s - 685s) ===

query so what does it do it will automatically generate queries from the method okay so we just write method jpa parts that method and create a query out of it need to follow a specific naming conventions why because see from method a query is generated and jpa converts it right so jpa follows a proper naming convention so that it can Parts the method so derive query used for get remove operation but not for insert and update because for insert and update already uh method is present here save you can use save to insert also to update also we have already seen that in the past okay but derive query you can use it get remove operation and you will get to know soon why only this so here if you see there is a Class part three. Java into an jpa framework if you search this class you will see a prefix uh you rejects so this is a rejects so this rejects so here if you see first is query pattern like this then is Count pattern this okay so all this get uh replaced here these are just a placeholder so ultimately this is going to be your rejects so here if you see that our method when we are saying that it has to follow a naming convention so this is it should follow this rejects then only your jpa would be able to convert from method to query so your method should start with either find or read get query search stream count exit delete remove okay so either one of them method name should start with either one of these value okay then the second part comes the second part says that uppercase letter like a BC anything you can use okay for example I have used let's say find find let's say I'm using find expense right anything starting with uppercase letter okay so uppercase letter anything so here if you see uppercase letter then I have put dot star question mark zero or more characters so I have put expense right so zero or more characters I have put after that you have something called buy okay so this is at the end of your method so find expense by now after this what you have to give is the Fe field name the field name of your entity okay so it will automatically create a query out of it it can par it so here if you see just see an example in this repository now I am writing one method find okay so this first one I'm choosing here from the find then capital okay then I'm uppercase letter I'm using capital u then s r anything like I want to give anything so here it's like zero or more characters right so I'm just using find user details then bu find user details by now here if you see this thing this thing ultimately after buy whatever you will write it will like wear Clause okay so this should be your entity field name so this is my entity user details inside that field name is name okay so it just follows like uh camel letter so uh n is capital so name okay and here in the parameter we have like what the name it should come so it will automatically generate a query like this select all the data from the user table user details table from this user details where username equals to this so this question mark will get replaced with this but now here if you see that we haven't wrote any J uh jpql or SQL query we haven't written any query we just written method but jpa internally Parts it into a query that's why this is known as derived query okay from method query is generated okay that is done by jpa not by us and this method should follow a naming convention like this so let's say different use cases so that you can understand more better about this uh derived query so now here see this method name which which I have written here so find okay user details by now I'm using name and phone so if you open that part tree. Java you will see all this parsing logic where like and or wherever it is present there itself so now here after buy you know that it would be a we Clause where then we have a name so this is all the fields now now okay so where name equals to this and your and your phone equals to replace with this so that query would be generated so and okay similarly or also we can use so now here see what I'm using by name and phone or user ID so the similarly it will get translated to query name and phone or user ID okay then we have something called comparison so the query can be so many things right so see if you open the part. Java class there is an enum list so because in the query we can have between is not null is null less than less than equals to greater than everything right so that's where we have all this enum present which we can write into the method so if you want to use let's say between so either you can write between in the method or is between both are both will work uh jpa would be able to catch both okay so these are all the kind of different comparisons or uh different enums which is also present so let's say that here I'm using find user detail by name okay and I'm putting is in okay after the field you can use anything it will just interpret like this from this name field okay where name field what we wanted is is in okay so we are since we are using in that's why the parameter acceptor list okay so username in and this whatever the list will go go inside this okay similarly you can use like also okay like name like okay then whatever the name would we have let's say string name so the similarly query would be generated like this I hope this is clear right so this is like jpa already has given us what else we can use in a method since we generally do not aware of this we find complicate to write a derived query uh but here jpa has given us everything like uh what should be the syntax we should follow and uh like in query what all things we can use I am totally agree also you might be thinking like we it is difficult to write a complex queries yes derived query is generally for uh not we can't write two complex queries right like join and all so it's just like like if you don't have that much complex but little custom requirement but simpler queries yeah we can go ahead with the derived query itself okay so here I using by name like and the name so now here if you see it got interpreted like this similarly now see we are using delete method so now whenever we going to use delete method so now we are updating the DB something so that's why we also have to use at the rate transactional on top of your method which you going to write so delete I am using you know right we can start with delete right and bu I want to delete by name and this is the name so here this query ultimately delete query would be generated like name so now let's say this you have let's say um name you want to delete a so if there are 10 entries with the name a so that many times delete it will call okay so all entries it will delete it okay we can also uh look at the
