=== @Modifying Annotation (3031s - 3403s) ===

is then there is something called at theate modifying annotation this is very interesting the jpql whenever you are writing any query the by default assumption is that you are going to write a select query okay so now let's say that I have written one delete or insert or add update query inside at theate query I have trying to insert delete something now if I run this it will throw me an error that hey expecting a select query by default jpql expecting a select query only so we have to use this annotation at theate modifying above at theate query one your query here and we are telling the JPI that hey expect either delete or update or insert query which is modifying the database okay so this annotation is used to tell jpa that accept either delete insert or update query so we are trying to update the DB so definitely with this three we have to use the at theate transactional also okay so like this the same query we can write it but since now it is delete we have to use atate modifying and at theate transactional also now there is one very interesting question also comes on top of it the usage of flush and clear like now we are using we are updating the DB with the jpql okay so we have data in persistent context also so how it impacts I will tell you so first understand the difference between flush and clear so you have let's say persistence context you already know that persistence contact it's like a first level caching and then you have DB let's say in DB you have entity uh table a and here also persistent contact you have entity table a okay here you have some value one here it has value two let's say because we are working on it we might be updating it so when you do flush so flush means push the changes of persistence context it's like a committing change it to two it's like committing but it do not delete the persistence context data when you do clear persistence context clear so it will remove it like remove this state of this a it is now not managing it okay why I'm telling you this okay so here consider this use case now so I am just saying that I am using add theate modifying and I'm using at theate query I'm trying to delete some data from the DB and at theate transaction I am using at the service layer and what I'm doing is first let's say uh you have a table user uh user ID let's say one so first I am doing find by user ID okay find and you know that when whenever I will do a find it will put into person persistence context now persistence context has id1 also now second I'm doing delete by username so I'm calling this in the same so it is in the same sequence happening right so I'm deleting the DB now so this data is deleted but now data is present into persistence context now I am doing find by ID will I get the data output present is it present true it's present because now when you're trying to do find it will not hit the DB it will return from here itself persistence Contex and data is present here so if your use case is that whenever I am updating the DB my persistence contacts also get updated properly so in the modifying we have to make flush automatically equals to True clear automatically equals to True flush means whatever the data is present into the persistence context update and then clear it also okay so now if you do that what we are getting first we have like say here DB id1 here we have a persistence context id1 let's say because we did a first find then we did by delete we deleted it and then we are doing when we deleted it we also put modifying flush true we haven't modified anything so there is nothing need to update clear automatically equals to true so it will remove the data from persistance Context now when you're trying to fetch from the that it will try to not uh fet from Pur C it will try to hit from DB and does value present now no false okay so if the question arise right when you are using at the modifying with jpql how you are uh managing the data in the persistence context we can say that by clear automatically and flush automatically setting it to true and also pagination and sorting in jpql is exactly similar what we have seen in the derived query so whatever the method you are writing where you are putting at theate query in the same method like uh you just put at theate pageable and also sort if you want and the same in the service you can pass it like in the derive we are passing it so no change in that it will work as it is so pagination and sorting is exactly the
