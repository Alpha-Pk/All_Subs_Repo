=== @NamedQuery Annotation (3403s - 3540s) ===

same now there is one more concept called at theate named query annotation so as the name says that we can name our query so that we can reuse it let's say that if you have any use case where a same query is used at multiple places in your code so instead of duplicating it we can write the query into one place using at theate name query we can give it a name and in our code base we can use that name directly so generally whatever the entity class which we have in that we write this ad theate name query whatever the query which you have to write write it here again it's a jpql for now and you can give it a name like this find by username you can give any name okay so on top of entity you have to use it like this given a name and the query whichever you want to write now in the repository or anywhere a different repository of user okay whatever the method you have at the top you have to write the at theate query right you now you don't have to write the query you can just use name find by username so it will automatically use this query itself right and the same name you can use at multiple places in your code base so that you don't have to duplicate your query and in future if any change in the query you have to make you have to make it only one place okay so that's uh I hope that's the purpose of it clear so in next video I'll tell you about criteria API specification API and Native query okay and some other topics but yeah uh that's pretty much uh sorry because jpa is going uh so many parts there are so many content uh or so many Topic in JP I don't want to skip any part so please be with me and I hopefully in the next part n itself I will complete it okay guys see you soon bye
