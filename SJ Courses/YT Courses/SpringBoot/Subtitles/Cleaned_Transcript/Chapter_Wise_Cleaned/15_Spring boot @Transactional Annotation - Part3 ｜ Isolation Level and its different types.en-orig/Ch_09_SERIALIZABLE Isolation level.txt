=== SERIALIZABLE Isolation level (1886s - 2326s) ===

read the last one is serializable what what serializable does is it there is no dirty read problem here there is no non-reputable problem here and there is no Fantom read it resolved all solved solved solved how the serializable same as repeatable log read log strategy so it it uses the same strategy during reading it requ it take a shared lock and release only only at the end of the transaction exclusive log is taken while writing and released only at the end of the transaction it does the same thing but it apply the range lock it apply the range lock now here if you see the Phantom read program I will tell you the same let's say this we will pick the same thing the two Row 1 and four status is free some status is pending now in the if you are if you are using the serialization uh isolation level if transaction one and transaction two are running transaction two let's say begin the transaction and it is doing this at time one read ID greater than zero and ID less than 5 so what it will do it will so read it is doing reading so means it will put a shared log and it will release only at the end of the transaction correct that's what it is does like repeatable read but one changes it put a range lock range lock means all the 0 to 1 2 3 4 so it will put a shared log here by T1 shared log here at time T2 uh not T2 T1 uh transaction T1 put a Shar log on this also it put a range log so this is kind of a range log range log means now 1 2 3 4 these all are kind of log now let's say at time two some other transaction has started begin transaction now wants to take an exclusive log and in insert a with ID equals to 2 will it be able to insert it no this will not be because at second row also range log is already present here which is a shared log which no other transaction can insert it in the mid between this one and uh ID greater than zero and five whatever the rows are satisfying this condition a share PA log would be put right and it's a range log means anything coming in mid of it also it will get log that's how it is solving the Phantom read problem because now no other transaction would be able to insert any new data in mid and so that now at time T3 if the same query runs again read ID greater than zero and ID less than 5 the same two rows will get return so there is no new rows will come because another transaction won't be able to insert because range log has been present all all this all this rows which are satisfying this plus any rows which are coming in mid those are get loged you cannot do any insertion in mid but this one thing clarifies you that the concurrency of serializable is very less or very low the concurrency Rises but the risk of certain problems arises like this so during which DB to use and which isolation level to use during transaction it is very important decision interviewer might this is very interesting topic for an interviewer that you know which isolation level should use so generally depending upon the requirement either repe reable read or repeatable uh read committed or repeatable committed out of this two only this is one is required because they give like some yes concurrency is there but also problem is solved so whenever this question come ask interviewer that okay whatever the application we are building non repeatable read is a problem or not if non- repeatable read is not a progam right go for read committed if it is a problem go for repeatable commit right so I hope this is uh clear to you but this uh if let's say that if you ask me right here sh like where this shared loog exclusive loog actually code is present see here in your application you will simply write read let's say uh select query you will write select query right select asress from your table where ID equals to 1 you will write only this query now depending upon the isolation level which your DB is using there is uh something called DB transaction manager right with each DB there is some transaction manager so this locking code okay I am using isolation level I have to use isolation level read so during application you just tell which isolation level to use hey use read committed then it will during select query it will make sure that it will put a shared lock and release it as soon as you read it so this taking a lock releasing the lock taking a shared lock releasing the lock is exist into DB transaction manager which is not uh like at at a application layer this is not uh visible to us it's abstracted okay we just simply write insert query update query but only isolation level is something which will tell to the DB transaction manager which ultimately talks to the DB but what locking it should use when to use when to release this is Guided by isolation level and that's where the if you read the definition again it tells how how the changes made by one transaction are visible to another transaction running in parallel I hope this definition makes sense now with that being said like the transaction management is a spring board I'm completed and see you soon with the next topic of the springboard bye
