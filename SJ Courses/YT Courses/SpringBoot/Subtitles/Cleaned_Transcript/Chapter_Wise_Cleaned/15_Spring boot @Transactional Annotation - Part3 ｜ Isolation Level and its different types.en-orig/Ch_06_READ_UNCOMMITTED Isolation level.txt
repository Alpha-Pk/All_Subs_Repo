=== READ_UNCOMMITTED Isolation level (884s - 1059s) ===

uncommitted isolation level solves nothing you will have a dirty read Pro uh problem you will have non repeatable problem and you will have panm read problem also why you can ask right interviewer can ask why read uncommitted has all three problems because the read uncommitted do not even take read LW do not even apply the right LW also so now let's say that if in DB I have an ID one and the status is free transaction one and transaction two transaction one reads it it do not put any log it just read it right and transaction one transaction two want to write it it can directly go ahead and change it there is no locking at all if there is no locking there is like whichever transaction want they can change it and the same dirty read program can come transaction two can change it to booked which transaction T1 can read it and what if transaction to got roll back the same dirty read can come similarly the same non- repeatable read and the Phantom read program whichever we have discussed above the same thing can come because no locking has been acquired by read uncommitted so the one follow-up question which comes also right then why this isolation level exist when all three programs are present no locks are required then why do this isolation level exist itself this isolation level is only used when you have only read application if you have an application where the use case is only read you have an static data and you only read then only this isolation level is useful otherwise it is of no used like it's very risky to use this because of all three progs which can come so it has to be used very very like if you're properly clear that these three problems will not impact your business logic or but you know right concurrency would be high concurrency is very high why because there is no locking no no transaction is stopping at all if they want it they can get the data if they want to write they can write it simply right there is no wait time for anybody there is no locking at the DB R but this is risky the another one is read committed so these two are pretty like you can say that mostly used right depending upon the use cases read committed and repeatable read so read
