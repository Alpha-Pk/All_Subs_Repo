=== REPEATABLE_READ Isolation level (1490s - 1886s) ===

repeatable read repeatable read solves dirty read problem solved also solve the non- repetitive read so these two are solved but it do not solve this Phantom read problem how the repeatable read so during reading it acquir the shared law and released only at the end of the transaction not like just after reading release it no once it taken a share log on a particular row it will release only at the end of the transaction similarly right once the exclusive lock acquired released only at the end of the transaction so the same you can try uh replicating it and understand with the same two transactions I have transaction one and transaction two right at time one let's say if this is the DB Row one and ID is one and the status is free at time one it has acquired a shared log on ID one so shared log has been taken by transaction one now let's say begin transaction now this lock will not be released until this transaction is not ended end can be happen through either commit or it could be roll back so let's say this is something whenever like let's say whenever whatever the time would be some I whatever the time would be till this time this lock is now not going to remove okay so other transactions can only read it you can only read it but can any other let's say in mid here in this time mid let's say time T2 can this take an exclusive log and update this one no because sh when the shared log is taken you know that you can't update it only you can read it so you you you will not get a exclusive log so no matter how much time you will read the same row I read again here at time T2 you will get the same stat here also you got free here also you got free if later on also you would do read here also you would get free okay so now it's you have resolved the non repeatable Problem by putting a shared log and keep it till the end of the transition no other transition in now between this time can change it right and also the dirty read also resolve because dirty read dirty read problem it's get resolved why because the log exclusive loog now let's say that the same row transaction one transaction two now let's say that transaction one wants to update it it take an exclusive log on Row one and wants to change it from free to booked so this will get to booked but here exclusive log is taken by T1 and this log would be kept till the end of the transaction either committed properly or roll back now transaction two in mid cannot even take shared log for reading or cannot even take exclusive log for writing so it cannot even read it so you dirty read problem is also solved till this lock is not removed and it will only get removed once it there is properly committed or it is rolled back properly okay but pantom read problem is not resolved how now here if you see that if you have two rows with ID one and let's say four here you have free here if you have let's say pending some status this is my DB table this is ID this is status okay so now what do happen is let's say transaction one transaction two at time T1 okay let's say you have read ID greater than zero and ID less than five so how many rows so you have put a shared log now shared log by T1 okay on which row on here if I put this shared log here by T1 shared log here by T1 both the rows okay so begin transaction so now this log will be kept till this transaction is not completed either commit or roll back now let's say this is at T1 and now let's say at time T2 another transaction started the begin transaction and take an exclusive log or you can say that doing a insert with ID equals to 2 so see 1 14 id2 is not there so some other row has inserted a new row two with ID new row is inserted two and some other status let's say free because see there is no shared lock on this right two part so any other transaction can go ahead and insert it and complete this transaction let's say properly committed okay now let's say at time three this transaction T1 runs the same query again again read ID greater than Z and ID less than 5 will it get the same two rows earlier like it get 1 and four now it will get 1 4 and two because this query is now satisfying this three so Phantom read problem is there still there in this repeatable
