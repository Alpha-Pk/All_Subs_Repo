=== DB Locking Type (Shared and Exclusive Locks) (616s - 884s) ===

about DB locking because this is also required so there are two types of DB locking one is shared log shared log and another is exclusive log so shared locks is also represented by S exclusive loog is also represented by sometime X okay so shared lock is also known as read lock and exclusive lock is also known as right log so now let's say that I have a DB in which I have one row called let's say ID is one and status is free let's say one is present so what is read lock or shared lock so now let's say that there are two transaction transaction one and transaction two so let's say that if transaction one wants to read this ID 1 it can take shared loog right I can represent the shared log with s take on id1 it has taken a shared lock on id1 So currently somebody is taking a shared lock on this one who transaction one now let's say that transaction T to T2 also want to read this row will it be able to read it yes because shared log can be as the name says that can be shared by multiple transaction but only for the reading purpose so transaction two also want to read it it can also put a shared log and it can read this same row so what shared log says that more than one transaction can put a shared log and you can read the value that's all you can only read but you cannot modify anything you can only read in other way the exclusive log let's say you have this id1 and the status is free transaction one and transaction two now let's say if transaction one has to change something in this row right and it don't want any other transaction should either read or either should write then it can go for exclusive log exclusive log on id1 so what exclusive log has been taken by T1 now let's say that if transaction to want to read or write on id1 will it be able to do that no because there is a exclusive log present exclusive log says that once the exclusive log is taken by any transaction no other transaction would be able to even read or write so that's where it says that if a lock is shared lock can another shared lock be taken yes you can take and you can read the value but if I have a shared log can another exclusive log can be taken no if any transaction has taken a shared log let's say transaction one has taken a shared loog right it has taken a shared log and if transaction two says that hey I want exclusive log on T1 can I take it it will check hey somebody has already taken shared log you can't take exclusive log only you can take shared log you can read it but you cannot write it till this shared Lo is present now if a transaction has exclusive log right if a transaction has exclusive log let's say T1 can another transaction take shared lock on it no when exclusive lock is taken you can't even read it you can't even write it so you can't even read it you can't even write it you can't do exclusive lock also okay so by this point of time two types of of locking and these three problems if it is clear then you will clearly understand what this isolations actually solves the problem so read
