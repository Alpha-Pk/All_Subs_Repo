=== Phantom Read Problem (496s - 616s) ===

the Phantom read problem the Phantom read problem says that if suppose transaction a execute same query several times but there is a chance that the rows returns are different itself then it is known as Phantom read program let's understand this definition with an example let's say at time T1 the transaction a started the transaction begin transaction and this is the current state of the DB current status is id1 is status is free ID row ID3 is status is booked currently let's say two rows are there so at time T2 transaction a is trying to read a row where ID greater than zero and ID less than 5 so what all rows will come ID greater than zero and ID less than five so this both the rows will come right so it will read two rows with ID 1 and three now let's say at time T3 some other transaction inserted the row with id2 and committed also so now let's say that this new this is the new row has been added by some other transaction at time T3 and also committed okay now at time T4 the transaction a is running the same query again okay read row with ID greater than zero and ID less than 5 now this time there is a chance that rows returns are different now three rows are coming ID 1 2 3 Because all three are satisfying this condition ID greater than zero and ID less than five so this is known as Phantom read problem when you are getting different rows itself with the same query let me also tell you
