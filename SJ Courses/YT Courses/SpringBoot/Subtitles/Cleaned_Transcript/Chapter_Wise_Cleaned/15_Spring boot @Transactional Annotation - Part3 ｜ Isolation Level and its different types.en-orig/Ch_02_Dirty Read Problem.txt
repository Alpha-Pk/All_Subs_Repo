=== Dirty Read Problem (209s - 384s) ===

is dirty read prog so what is dirty read problem dirty read problem says that transaction a read the uncommitted data of other transaction okay and if other transaction get rolled back the uncommitted data which is read by transaction a is known as dirty read now what let's see this definition with an example so let's say this is the time this is transaction a this is transaction B and this is let's say a current DB status at time T1 both the transaction started in parallel begin transaction begin transaction and this is the current state of the DB id123 and status of the column status is currently free at time T2 transaction B is updating the row which has ID 1 2 3 and changing it to booked so so DB status is currently 1 2 3 and status is booked but at this point of time transaction B has not committed right it is still in progress transaction is still in progress it is not yet committed or roll back it is still running okay so this status booked this change by transaction B but it is not committed this transaction is not committed so at time T3 the transaction a read the row with ID 1 2 3 so in the dirty read the transactions also read the uncommitted data so even this status which is change by transaction B is not committed yet still it reads it so transaction a is able to read what is the status book okay now at time T4 now let's say transaction B some issue has come and does the roll back and now you know when the roll back happen all the changes which it has done they again goes back to the previous state so it again goes back from to free but the problem here is transaction here has read the uncommitted changes booked which is now not there into the DB this is known as dirty read transaction a is is now suffered from a dirty read problem because it has read a row which has uncommitted data right so the transaction which is not committed all the changes is also read by other transaction then it is known as dirty read problem okay so if this problem is clear let's move to another program which is
