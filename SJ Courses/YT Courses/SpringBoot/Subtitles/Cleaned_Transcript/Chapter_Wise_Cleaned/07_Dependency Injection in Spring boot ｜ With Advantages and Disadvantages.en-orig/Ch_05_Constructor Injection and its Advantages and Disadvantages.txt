=== Constructor Injection and its Advantages and Disadvantages (1147s - 1649s) ===

construction injection which is heavily used or you can say that it's the most recommended so dependency get resolved at the time of initiation of the object itself this is the best part it's not like after user object is created then spring uses reflection and then fill up this in uh dependencies inject the dependencies no at the time you are creating a user object itself this dependencies are also resolved how so let's say that this is the user class marked as atate component this is my order I haven't put at theate Auto here just a normal order now this is the Constructor now you know that Constructor is used to creating an instance of an object so in this Constructor you will pass the order for which dependencies has to be resolved and here I am setting the dependency this do order equals to order and I put Auto wired here so this Auto wired ultimately get applied to all the parameters if you have more parameters like this this Auto get applied to all so now is spring would resolve the dependency of all and then initialize it at the time of user object creation itself and also one more interesting will tell you that from the spring version 4.3 right when only one Constructor is present then you don't even use autowire right that is also works fine it is not mandatory when you have one Constructor so here if you see that in this user class I have only one Constructor right so now when the spring initiated it I haven't put autowire also so even though it will work fine and if you have more than one Constructor if you have more than one Constructor then Auto add usage is mandatory you so now let's say that I have two dependency on order and invoice so I've created two classes one is invoice one is order very basic right uh at theate component at the lazy I have just to make sure that when they should get invoked when actually they are used so user has this two dependency so this is my first Constructor which resolve this order this is my second Constructor which accept the invoice and putting the invoice now there are two Constructor and I haven't put an autowire now this will fail if you start the application Bean in initiation exception will come because there is no default Constructor and after that it has to use one Constructor but there are more than one Constructor so spring doesn't know which Constructor I should use so it will throw exception that you tell me like which Bean to use which Constructor use so you have to tell one autowire on one of the Constructor so now let's say I have put on invoice so what would happen first user object will get created so invoice let's say during ioc started they will find Bean of order uh class for this but since it's Mar lazy it will not get invoked till it's not used so it found user it will now sees that okay this is the Constructor which I have to use so now here if you see that I am trying to use the invoice so first invoice will get invoke invoice initialized after that it will put here invoice injection is set after it says that user initialization with only invoice and finish so order is not even touched okay so you got the Constructor injection but now you should ask me that hey sh why it is recommended what are the advantages of it so first thing is all mandatory dependencies are created at the time of initialization itself so it makes 100% sure that our object is fully initialized with mandate dependency second part is you can make your field immutable so now here if you see that I can use final and in the Constructor you can initialize it because at the time of initialization creation of this user itself I am initializing it only for the first time after that you won't be able to change it so this is another big advantage of it and with Auto on the field injection I already told you you can't use final but with construction injection you can make your Fields immutable you can put final and during object creation itself you can initialize it for the first time and after that you won't be able to change it third Advantage is fail fast means that if there is any missing dependency it will fail early so here if you see that at this point of the time let's say that I am creating this user class and I bu mistake missed autowired when you bu mistake missed autowired what would happen is a spring will create an object of this user it will call the by default Constructor this order object would be null and if somebody's trying to access it like let's say order do something it will call null pointer exception so you are getting to know about this at run time not at compile time but with Constructor injection like let's say that even if you missed Auto wire here like even if you missed autowired on the Constructor you know that if for one Constructor spring will automatically resolve it and fill it if you have more than one Constructor let's say then spring will fail it I told you that it will fail be initiation exception that which Constructor to use right so you have have to first put Auto wire somewhere right so it will force you it will fail fast during compile time itself unit testing become easy that the same as the seter injection right like you have an order object you can inject it here in the Constructor so I'm creating new user you can pass the mock object here you don't need at theate inject marks at theate Mock reflection to set the this mock object here in this you can pass it here it will get realized so unit testing become little easy so these are all the advantages of Constructor injection now you can ask me that hey sh why you haven't tell me that what is the disadvantage of Constructor injection because I will tell you that the disadvantage of Constructor injection is also I will consider mostly as an advantage so one uh generally one disadv Vantage of Constructor injection is hey if you have let's say that 20 dependencies your Constructor will become huge let's say object one object two object 3 object four like this and you have put Auto here your Constructor parameters will keep on increasing so this is the one of the disadvantage but here if you see that I considered as an advantage because this will keep you a check that hey is your code design is good that your parameter you your class has so many dependencies I think it's a good time you should refactor it so what are the some of the common issues when dealing with dependency injection so two major issue is one is circular and another is your unsatisfied dependency so let's first see the circular
