=== Setter Injection and its Advantages and Disadvantages (939s - 1147s) ===

injection so Setter injection is that dependency is set into the field using Setter method so it means in the setter method we are putting an auto wire not on the member variable field so here I am not putting autowire I have just created the field and during this I have created one set method set order dependency like it accept the parameter order that this dependency it has to now initialize and put autowire mean that this method should be used by Spring to create the dependency inject the dependency into the order right so what is the advantage very simple simple simple as uh field right uh the same but only instead of Auto here we are putting an auto here so what's the advantage Advantage is that dependency can be changed any time after the object creation it means that for now you have dependency let's say you have put anything since it's a Setter method right you can always call this method pass different dependencies here and it can be changed let let say you have order online order offline order let's say that some other type of order so let's say you have the privilege now to first set it through online then first set it through order offline so it says that dependency can be changed any time after the object creation once the object is fully created spring might fill the instance but you also have now leverage that you can again call this method and pass whatever you want maybe if you want to change it okay second thing is easy of testing so now in this Setter method in this parameter you can pass your mock object mock order object which get set here now you don't need at theate inject MOX atate Mo which uses internally reflection you can pass it and ultimately it would get set okay but there again it has some disadvantage one thing is you cannot make field immutable like you can't put final over this why because in the setter method you are setting it you cannot field cannot be marked as final because once user object is created spring will use the reflection to find out hey is there any methods which is marked with autowired it will find it and now spring would be able to initialize it right so field cannot be marked as final here so you will see that here you will see red line like this difficulty to read and M so it's very difficult to read and maintain that okay hey this somebody created this field is it managed by a spring or not so it has to look for any method that which has Auto on top of it so this is not readable actually right and it is very difficult to maintain like okay if somebody has missed this meth itself or not so these are the you can say that the disadvantage of the setter injection then here comes the
