=== Why Authorization Header is used for credentials (304s - 464s) ===

Okay. So one question comes to our mind that hey Shange why we need to send the username and password in authorization header only why not I can't pass it in request body or query par parameter or any other way like why only header so these are the three possible reasons in my view. First thing is standard we have to follow one standard. Let's say if you don't follow any standard one client can pass you into a request body another client pass you in query parameter another client pass you in header so it would be very difficult to deal with different clients okay so there is an RFC 7617 HTTP standardization there it mentioned that all the credentials has to be passed in header only and it is important for universally accepted standard across APIs and client so that no matter what clients what APIs if you have to pass the credential you have to pass it into header so that's a standard so that you can write frameworks on top of it okay the second thing is security there are many web servers which log the request body and even the query parameters okay and that is not safe because they might be logging it for debugging or analytics purpose Because what if your username and password got logged inside that right and you know that sometime the logging tools which we use in a company it's belongs to a third party it's not yours okay so this is a security breach but generally headers are not logged we never log headers right so it's reduces the risk of exposure okay so security is one another big reason why header is chosen that yes we can pass username and password inside an header and the third one is support for all HTTP request in my view if you see that post and put yes we have a request body and uh we can pass it but what about the get generally get do not have body so generally if we have a header then it can support whether the API is get put post whether the API accept body query parameter or not. So header would always be there. So it to maintain a consistency also this uh required. So this is my reason like why I think that authorization header is preferred for the username and password. Okay. If that is clear now
