=== Servlets (0s - 630s) ===

hey guys welcome to concept and coding and this is Shan and today we are going to do the first topic of spring boot that is Introduction to Spring Boot and I will cover this thing what problem it solves and its features and advantages so let's start without wasting any time so why spring boot exists today what problem it solves right how it is differ from Spring framework there are lot of questions about it so to total clarify about it like the complete picture we have to go little bit back when seets were popular so back in 2015 and 2016 I worked a lot on sulet and I know how hard it was to manage the application and currently we are working on a spring Boot and we know that how easy it is compared to sulet so I will not tell you about in depth depth about sulet but at least I will tell you what problem was exist in the sulet what problem it solves okay so let's go so that's why I mentioned before we talk about a spring or spring boot first we need to understand about sulet and sulet container so these are the foundation for building web application back in 2013 14 15 this suets are very popular and these are you can say that the foundation for the web application spring and spring spring boot internally uses the same concept right so they sulet actually build the foundation so what is sulet sulet is a Java class which handles client request processes it and Returns the response so considered it as a class where you have Define any one your API input request comes in this is known as sulet uh this API got invoked it will do some processing and return the response so this actually which handles the request processes it and Returns the uh send the response back is known as sulet and what is sulet container a sulet containers are the one which manages the suets so there can be so many suets so sulet containers are the which manage this suets which manage this suets okay so I will come to this diagram but let me first tell you in a very easy manner how we used to write a web application very simple only one uh you can say that one class I will tell you so this is how suet used to look like okay so I have defined my uh AP API here so here at the web sulet SL demo sulet right one demo sulet one so this is you can say that my API and I extend it with HTTP sulet so a one sulet can have only one do get do put do Post do delete right uh these are the in uh methods which we have to override and Pro the implementation so here if you see that this is the sulet and in this method any get request which comes it will comes to this so it will get comes to this I am doing processing and ultimately will send the response if required So currently I void no response so any response will go so HTTP response will go in the this response I can set it here okay so this is known as suet so this is I make another subet because we can have more than one suets so that's why I created another subet and give them the name demo sulet 2 okay so in this sulet also I can have do get do put do Post do delete right and do something and return if required so I can have multiple cets so there is something called web.xml and this is the culprate actually which makes everything too complex so what in the web.xml there are so many things we used to do filtering and all right but let's keep aside uh the one thing which we have to do is the sulet mapping so what sulet mapping is that so this is one sulet demo sulet one right so this is the sulet name demo sulet one what all URL this will handle okay what all URL this will handle so let's say slash demo sulet 1 this if if customer hit this URL let's say demo sulet 1 okay so it will comes to this subet so this subet will handle so that's what its mapping is if user invoke this API SL demo sulet one slash first endpoint some endpoint which subet will handle there are multiple subet subet one subet 2 subet 3 subet 100 so if customer hit this API which sulet will handle so I have mapped it to sulet one this sulet so that API will come to here this sulet one and after this it will go inside a particular method if user has hit the get HTTP request get so it will go inside do get and check the request info okay first endpoint SL demo sulet 1 and user hit first end point so okay this LF will get executed okay and if user hit this API if user hit this API so in the sulet mapping web.xml it has been mapped to which sulet suet demo sublet one so it will goes to the demo sublet one and let's say it is again a get call so it will goes to the do get and then it will check all the if condition okay uh this is the this LS if condition got matched SLC and point so this this processing will start so you got it if if the put API got invoke put slash demo demo sulet 1 so what would get invoke okay demo subet 1 is the API which is mapped to this AP this sulet okay this sulet will invoke this is the put so it will goes to dop put and then it will see okay this is the path and do the processing so similarly another mapping I have done is for my second sulet so this is how sulet used to work now let's come to to this diagram so when input request comes in it first has to goes to the Tomcat this is the server which is also known as actually your seret container right this is not service container uh my bad it's serlet container so Tomcat used to know uh also known as serlet container okay so we have to deploy our application here in the Tom sulet container so our application has all this sulet subet 2 and all this we will convert it to war and we will deploy it here in the tomit now what this suet container will do I told you that subet containers are the one which manages the sulet so what it will do is at the first it will invoke use the web.xml and see that hey user has invoked demo CET 1/ first end point who so now this sulet container will use this web.xml this mappings and identify which subet has to be invoked so in web.xml it will check that okay uh demo subet one first endpoint yeah demo sulet one uh first endpoint this one which sulet going to be handled this is the name sulet name demo sulet 1 okay so this is the name demo sulet one which class demo sulet so I have kept the name and class name same so this is the name you can give any name so with this name it will find out the mapping class so this class will get invoked and this class is the sulet class so it now know which sulet has to invoke and then ultimately it call the sulet right so it will call this demo sulet one right that particular method because it knows that it's a get call right so it will invoke its do get method and now this sulet would should be able to handle it okay so this piece of code would be executing it and it will send the response back so you got the how subet used to work now there are certain problems in it which spring frame framework solves right spring Frameworks is you can say that is a family which has lot of features right and you can say that even spring boot is comes under this spring framework only okay but I would say that before spring boot comes there is something called that spring MVC this comes before even spring boot okay so we will come to Spring boot
