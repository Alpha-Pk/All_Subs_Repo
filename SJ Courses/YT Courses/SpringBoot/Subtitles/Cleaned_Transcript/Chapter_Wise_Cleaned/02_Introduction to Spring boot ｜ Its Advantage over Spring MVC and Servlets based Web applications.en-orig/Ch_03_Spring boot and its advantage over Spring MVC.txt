=== Spring boot and its advantage over Spring MVC (2035s - 2752s) ===

boot is required what problem is spring boot solve why it exists today spring boot Sol challenges which exist with spring MVC three big things first dependency management here if you see that in the spring for.xml spring MVC is spring MVC if you focus a little bit you are adding a dependency whatever you required and you also have to provide the version you also have to provide the version now let's say that in future you have to upgrade the version of J units from let's say 4.3.2 to let's say 5.1.2 something but will this version work with this version also it it might it might not because you know that the dependencies should be compatible with each other so this is one problem with the spring dependency management so no need for adding different dependencies separately and also their compatible version heading Springs keep it easy so spring boot keep it easy you have to just give like this starter web and starter test so this web already internally it will load whatever dependency it require to create a web application this will load whatever the dependency it required for testing so it will automatically add the dependency for you and here also if you see you don't you are not defining any versions also so you don't have to worry it will automatically uh load those version which are compatible with each other and internally it will load all the dependencies which might required for creating a web application here you might require 1520 dependencies I have written only three but it might require 1520 but here you might only need two internally it will load all in here you need is that this parent that's all what spring boot version you are using three version that's all you only you have to define the spring version here starter parent version rest all this thing you don't have to define the version itself internally it will keep loading keep adding the version it is now spring boot had so dependency management is the first advantage of a spring boot over spring MVC okay so understand this that this is your spring MVC this is your spring boot so all the benefits of spring MVC is also present into spring boot plus additional something so dependency management is same thing it is doing the same thing spring boot is doing the same thing as spring MVC but now it's a little bit abstracted to us second thing is second Advantage is auto configuration here if you see that this dispatcher solet this is kind of a configuration right this class is kind of a configuration this class is also a configuration web mnc's app config right these are lot of configuration class so that's why the another advantage of spring board is auto configuration no need for separately configuring dispatcher ofet app config you don't need to Define component SCAN Web uh enable web MVC is spring boot internally add by default values for it so spring boot internally will add by default values of at theate component scan so wherever your application uh start applic ation exist main method from that package onwards it will start so what it does is uh there was a term called if I remember correctly there was a term called op op opinion opiniated just double check once I think that's why we call that spring boot has opiniated nature means it will give opinion so it opinion means it choose by default hey component scan I am choosing the package as from wherever your application main method starts that package I will choose so if most of the time you can go ahead with this spring board opinion okay so that's why you don't have to write this but let's say that if you are not agree with spring boot opinion or by default you can anyway add this your own also I will tell you you can add it but that's why it is known as Auto configuration means no need for separately configur configuring all this dispatcher solet app config component discan spring boot add internally by default but if you want you can in this springboard class you can add component scan if you want to change the default values right we will see that uh at theate component scan what does it mean and all don't worry about this but understand what is mean by Auto configuration and there is a term called I think opiated that's the same thing spring boot gives the opinion that I am using this so if you are agree with this opinion you don't need this configuration or it's autoconfigure if you deviate from its opinion you have you can write component scan and give your package from where to start so only one class you required here in Spring boot is this spring boot this annotation spring boot application annotation so this annotation itself internally has at theate enable web MVC atate component scan dispatcher sulet all the configuration Auto configuration are you can say that hidden inside this one annotation itself and this is uh your main method the start of the application okay so second ADV Vantage is clear Auto configuration there is now you don't need to create this class you don't need to create this class you don't need to do at theate component scan everything is taken care by Spring boot itself so now it's very clean class one and the third very big Advantage is embedded server so from if you remember I was focusing on the term that we have to deploy or we have to first create a v of our application and deploy that into the Tomcat which is a sulet container okay in both sulet and spring MVC but in Spring boot in Spring boot server is already embeded you don't have to generate a VAR you don't have to take a VAR and deploy it to a tom cat or subet container server is already embedded inside it it automat atically does it for us we don't have to do it right seret container is already embedded we don't have to do all this stuff just run the application I will tell you how so what is a spring boot in simple term it provide a quick way to create a production ready application it is based on Spring config spring framework only spring MVC it support convention over configuration I already told told you that auto configuration part right it uses default values for configuration and if developer don't want to go with convention they the way something is done they can override it convention means the way something is done default values if you don't agree with the default values you can always override it that is convention over configuration means so it support convention over configuration it also helps to run an application as quick as possible right so now this is very simple here if you see that form. XML is very simple here parent. parent is there where with the version but now it's dependency here if you see that only two dependency you don't have to add more you don't have to add version also so you don't have to worry about dependency management there is nothing no configuration you created One controller class I have defined controller class I have defined the mapping which API uh one get call for sample and this is the start application I told you why it is required all the auto configuration here hidden inside it now now when you start this application this method and if you on the Chrome if you just call this if the client is invoking this slash my API SL first API spring boot is nothing but a spring MVC right the same dispatcher subet is hidden to us but internally dispatcher sofl will use this mapping it will identify okay my API which controller okay my API is mapped to this my controller okay first API which method this get method will get invoke and it will print this hello from concept and coding so dispatcher subet will create an object of it and it will pass it to ioc if there is any depend to resolve any dependency if there at the rate autowire nothing is there so no need of ioc it just simply create an object of this and invoke this okay so let me show you here this is the spring boot here if you see I am just simply running run here if you see that Tom cat is started so I don't have to create War I don't don't have to deploy automatically server started on this port now we can just simply go and in the Chrome we can add this API Local Host uh this is on this is the port where it is started and this is the API I am hitting so this is the API I have return controller my API and this is the endpoint first API if I press enter hello from concept and coding hello from concept and coding don't worry about this setup of this project that we will see in the uh second part uh second session but focus on the points like advant why spring boot needed at the first place that was the big I can tell you just three advantage of this embedded server Auto configuration dependency management and I can simply cover this whole video in 15 20 minutes itself but with without understanding the suets what problem exists what spring MVC solves it and what are the problem in Spring MVC itself which spring board solvers without that it's a partial knowledge but I hope you uh like it and you will share it with uh your connection if you think it will help them so thank you for that and see you in next video bye
