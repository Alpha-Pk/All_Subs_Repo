=== Spring MVC and its advantage over Servlets (630s - 2035s) ===

so to solve the issues of the sulet I will tell you the issues spring MVC comes into the picture so what all problems it solves so spring framework solves challenges which exist with suets what first removal of web.xml this web.xml becomes too big so because see when you work on a production level application where it takes handles uh millions and billions of requests this one subet two subet there can be hundred of suets and to have the mappings of those hundreds of subet it become too big right also this web.xml holds lot of information like filtering any request comes it has to go through filter what sequence of filtering it has to go that also maintains over here so this web.xml is contains lot of information and it become very difficult to manage and understand it so spring framework introduce annotation based configuration so removal of web.xml completely makes developer life lot easy because now developer can work through annotations okay okay so the the second advantage of a spring framework is inversion of control very important ioc so what ioc definition is ioc is more flexible way to manage object dependencies and its life cycle through dependency injection so sometimes you say that hey dependency injection and sometimes some people say ioc both are same so generally you can say dependency injection is an implementation of ioc okay so sometimes somebody calls ioc sometimes somebody say dependency injection but they both are same okay so what is uh inversion of control uh I'll I I'll tell you here let's see this example then we'll come back to more advantages okay so here if you see that consider this example simple Java class okay in this simple Java class I have created a payment class and I this payment cloud is depend on this user so I am creating an object of this user there is a simple method get Sender details ID so what I'm doing is I'm using this object sender calling its method get user detail and passing the ID so what it will do is it will invoke this user class this method pass the ID and it will fetch certain data of this ID and maybe return it or do something okay so what is the problem you see I see there is one major problem is very tight coupling so there is a very tight coupling between payment and user class you can ask me right hey sh what do you mean by tight coupling here so see this is I can tell you what like what I mean by tight coupling suppose I want to write unit test cases for the payment get Sender details method you already know that unit test cases means only if let's say if I'm writing a unit test cases for this payment class right so I should have the capability to test this method only this method if this method is invoking some outside different class method I should be able to mock it mock it in the sense okay send. dot if whenever this is in called let's say I should always get this value so I mock this that's what unit testing is right you should be able to mock the data which you are not testing okay so as in the mock testing of this class and this method I should not test this so that's why I should be able to mock it but will you be able to mock if you are creating a new object here if you are creating a new object here it will definitely call this for sure so how you will mock it there is no way right so definitely writing a unit test cases would going to be very very difficult so how the inversion of control or dependency injections solve it so ining framework again was don't worry about this annotations I will explain The annotation in the upcoming session but this uh component autowired what is the usage of this component just keep aside first but just understand the concept right so with the dependency injection what problem we solve is so now here we have used certain annotation which tells the spring that hey you manage it okay so here if you see that user I have given atate component and now this I handle it to the uh spring hey you manage this Bean okay you manage this class similarly payment also I have created at theate component and this class is also now get managed by spring and here I have put at theate Auto so Auto tells the spring to resolve and add this object dependency whenever this class get initiated okay so now whenever this class get initiated what spring will say that okay at theate Auto is there means I need to create an object of it okay so it will resolve it spring will resolve the dependency it will create object we are not creating object here like this okay so what advantage we get here the advantage is let's say that first unit test cases unit test cases I can mock it I can tell a spring H create a mock data for it mock it now during run time a spring will create a mock object of it and using that mock object I can say that Hey whenever that Mo mock user object do get something has been invoked do do this okay so what happened is that now we have the control okay so here I haven't hardcoded it like new user which always create a new user I now this is getting managed by Spring and this process is known as dependency injection unit testing is much harder in sulet and I I already told you how it become easy in the spring through dependency injection we have already seen the process difficult to manage rest API as you already see that each sulet can have only one get method one put method one post method right one particular solet but let's say there can be for this subet let's say there could be 10 apis which has to handle so generally this is how it should it we used to do if else if else if okay so in this path which is handled by this of whenever this path comes do this whenever this path comes for the get do this whenever for get this path come do this so it's not very you can say that uh flexible way to handle the uh input request which is coming right so path mapping makes code little difficult to understand that's what I used to feel right so spring mvy provide an organized approach to handle the request and it's easy to build restful API I'll tell you like very very simple we are also using it right uh very easy way so here if you see I have also taken an example this one so we we have like at theate get mapping this path now you can have another at theate another get mapping let's say at theate payment some certain my endpoint certain endpoint let's say one right so you you uh and it's method public string public string some let's say method now do something so in one class we don't have to have like all the get apis if else if else if else you can have proper if there are 10 get apis you can have 10 mappings properly so it's very easy to understand it's uh you can say that more organized way of handling the request okay so it's easy to build restful apis so there are many more advantages of a spring framework and the spring framework brings lot of other Advantage I have already told you that integration lot of Integrations are present okay developer can choose different combination of Technologies and framework which best fits their requirement for example for unit testing you can choose junit mockito integration is available in Spring you can start using it let's say for data you can use uh hibernate jdbc jpa lot of integration available if you want hibernate based upon your specific need go ahead use it if you want jdbc go ahead use it similarly for asynchronous programming caching messaging security all Integrations present this will take your picture Cod clear your picture completely okay about this spring framework so request comes in again like a same here we have something called subet container so tomit is the subet container we have to deploy our application here remember the word I'm saying we have to deploy right so whatever the application we have we have to create a war deploy it here in the subet container Tomkin okay why I am focusing it more because it will it's an advantage of a spring boot I'll come to that so in the spring framework or you can say that in the spring MVC earlier when request comes in it goes to the subet container in the subet container which is uh Tomcat we have deployed our application then it the request goes to dispatcher subet it is also known as the first controller also known as first controller dispatcher sulet okay so in the spring these classes uh where yeah these classes right where we have exposed the apis right this is known as controller right so we can have so many controller classes right let's say I have this uh payment controller right I have this payment controller class I have put The annotation controller to tell the spring that this is the controller class so any API will come you will find certain mapping here so similarly I can have so many controller class let's say that class order order controller I will also have annotation at theate controller here sales controller something so so many controller class there so which controller class to invoke for this API so let's say that the request come is slash uh demo slash .1 this API comes now which controller will handle it in the subet this is going to do that stuff subet container but in Spring no spring there is something called dispatcher ofet so this subet container pass it to dispatcher subet all the end points now this dispatcher ofet which is also known as first controller will decide which ultimately controller I have to pass this require and how it will do so first it will choose the controller and it uses Handler mapping okay so here we are doing request mapping here if you see these are the anot I told you we are not doing web.xml we are not creating web.xml in Spring framework annotations we are using it so you can just see that internally it might be doing certain XML configuration and all but for us it's a simple annotation that we have mapped this class we have mapped this payment controller class to handle this request mapping at the rate payment API okay so like this mapping we have done so first dispatcher sub dispatcher subet uses the Handler mapping to know which controller to invoke now it knows that okay uh for example payment API now let's say instead of this demo it is/ payment API now based on this uh Handler mapping it knows that okay okay it has to invoke this controller payment controller okay now it has to create an instance right because I have uh because here in the spring we cannot you won't find anywhere uh equals to new equals to new so dispatcher ofet is now going to create an instance of what create an instance of your controller class payment controller so now now it will create an instance of your payment controller and it will use it will use or you can say that the leverage ioc inversion of control now now what it will do is that okay I need to create an instance of payment controller but for for that I have to also resolve the dependencies if there are any so it will see that okay ioc now it will goes to the ioc ioc uses dependency uh injection and with that it will say that okay at the rate auto means this payment controller is dependent on this object I have to resolve it so now ioc will create an object for this and inject it right so it will on the fly it will now create an object of it and populated with the object okay so now the instance is created for this and all the dependencies are resolved for this which are where this class depend on is all the auto wires are resolved by whom by ioc right all the dependencies are injected now what it will do is it will dispatcher will invoke the controller method particular method so now that particular method let's say the API was uh end point is let's say slash payment API slash payment this is the request so it will say that okay SL payment it will invoke this particular get mapping okay so here if you see that these mappings helping the dispatcher subad to determine which controller and which method to invoke which controller and which method to invol okay so is it clear like when I'm saying Handler mapping what does what does it mean this mappings request mapping get mapping these are ultimately helping the dispatcher ofet to determine which controller to call and in this controller which method to invo based upon this API which is coming it will do this mappings and find out the controller and Method so ultimately in unvoke the controller method so this will now process it and then ultimately return it and it goes so spring framework is clear right where the ioc comes into the picture actually right okay so till now you clear that from the journey from sulet to Spring framework which is spring MVC see you know the advantage now why what's the need of a spring boot now what's the use of spring boot but before uh spring boot uh let's see the one example of uh spring mvca so that will clarify when we will see the advantage of a spring boot so this is a very very simple spring MVC application you can say that okay I have created a controller class One controller class like P controller put again ignore the annotations we will see The annotation in depth but for now just remember this is one of the controller one one of the controller class where kind of a similar to sulet where any request comes in processes it and return it okay and these are the mapping in sulet we were doing a mapping in web.xml in Spring MVC these are the annotations we are using and it mapping whenever any API comes in so these mappings helps which controller should handle it and what method and dispatcher ofet is going to use it so this class is created this is something called pom.xml where we have written the dependencies so this is first dependency let's say we are creating a web application web uh spring uh MVC so we have created its version then you want subet you have created its version added dependency let's say if you want unit test cases junit you have added its dependency with the version so for simpler perceptive I have written three dependencies uh you can write into the pom.xml this is May one right so I have written three dependencies the another class is app config so this CL in this uh class generally in the spring mcy see we have this annotation important where what this tells is that it loads lot of dependencies which spring Embassy is required right so it loads all of it then another annotation you atate component scan so we provide a package so what is this only within this package you are telling that hey Spring only within within this package and Below you go and manage whatever the controllers or whatever the controllers I am creating it right uh controllers beans and all so we are telling spring to do a scanning whatever because uh spring has to search right the controller and everything so we have to give it a path from where it has to start searching for the component scanning so we can provide it here so that's where it's known as configuration classes uh because it load configuration for spring embassies which is required to work and also component can we Define it here and I told you that dispatcher sulet is used in Spring MVC and this dispatcher sulet is the first controller all the request from subet controller comes to it and it it perform all the stuffs select choose the controller create an instance so that's why there is one called dispatcher sulet class we have to create like this in Spring MVC we have to extend this uh dispatcher ofet this one and we have to provide this three methods we have to override this three methods provide what is the config class we can provided that this is our config because this class name can be anything right so we have to give like what is is the config class so any dependency it required it can uh load it enable MVC right this is an annotation uh so this annotation load lot of spring MVC kind of libraries internally then subet mapping so we have told that any request coming with this slash itself you have to handle it right so those also we have mentioned in this dispatcher subet what all request we it has to handle so this means all okay so this if you see that even a basic application I am writing I have to have one controller this form this config and this dispatcher subet and you already know what is the dispatcher sulet does controller and ioc and all so this spring framework is clear now you can tell me hey sh why is spring
