=== What is Persistence Unit in JPA (1892s - 2175s) ===

first so it's what it's a logical grouping of entity classes which share same configurations okay so persistence unit means let's say I have 100 entities I have 100 entities entity one entity 2 entity 100 so let's say this one to entity 40 shares the same configuration means they they want to go into the same DB let's say S2 now entity 41 to 100 are sharing the same configuration they have to go inside my SQL let's say okay so what we do is one persistence unit means logical grouping of entities which share same configuration so we can have one persistence unit for this one persistence unit for this so if you're not using a spring boot generally you create a persistence.xml file like this so you create a persistance unit you provide some name here you provide a transaction type we will see what is this transaction type resource local means 1 DB transaction is Scopes to 1 DB so from the previous transaction uh video you know that right whenever we create a transaction begin commit its scope is specific to 1 DB only DB1 okay if you have more than one DB db2 the same transaction transaction one will not work okay it can scope to only local to 1 DB so generally that is resource unor local if you want transaction to scope to multiple DB we have to use JTA not resource Loc but we will see later so we will uh type what is the transaction type we will group all the entities which you want which is going to share the below configuration so let's say I have created two entities sample entity one sample entity two so this is like a grouping then what is the provider I want to use so here I want to use hibernate provider you can even use Eclipse link uh open jpa like this currently I'm telling you if you're not using springboard you have to Define like this and the properties properties like what is the dialect what is the driver DB username password like this but in Spring boot we don't create persistent. XML if you have only one DB one application One DB you can use application. properties inside this you have already seen how we can create data source we can create DB driver user us name password okay and here generally if you see this is optional we haven't defined previously okay but you can provide a package to scan so it will this all entities which is in this package it will scan okay but generally this is not required because spring Bo look for all atate entity annotation so you don't have to provide a specifically provide a package okay then this is also not mandatory that you don't have to provide what is the hibernate provider what is the jpa provider you are using if you want you can specifically provide hibernate persistence provider if you want to override it but if you don't provide it it will automatically use this similarly what is the dialect you want to use provide a proper dialect okay uh dialect you already know what is used for converting the jql or hql into the s but if this is also an optional because if you don't provide it spring boot automatically pick uh appropriately and similarly you don't have to provide transaction type this is the default it would pick until unless you want to override with J uh JTA so resource local is uh whenever you create a transaction begin commit it's always the scope is to 1 DB only okay so that's why we haven't we only Define this we didn't Define all these things the spring would automatically take care of all these things okay and always remember that application. properties means only one persistence you can have means Only One DB
