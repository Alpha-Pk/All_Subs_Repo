=== SQL Injection (1251s - 1491s) ===

itself and the last one is SQL injection in this one attacker manipulates SQL query by inserting malous input into the user field so I'm creating a very very simple here let's say that search field now whatever you put search let's say that uh cold drinks what if you are using this input and putting into query select estess from products where uh type equals to and this whatever it is coming search value you are directly using whatever user is putting into your DB query so here I can put some malous uh you can say that query which can even like give me the data which for which I'm not even authenticated or even I can delete your DB also or get your DB table name complete right so with SQL injection like your DB is not safe even I can get what is the table what are the column name I can delete it or I can access the unauthorized data also from the DB so in this example I'm showing this only very simple find API and I'm using this whatever the name is given by the user I'm directly putting into the query okay so now here if you see in my DB currently there are two users okay and my query says that whatever the username comes only that would be return if a a a comes only that should be written but now let's say that what if I put hit the query like this SL find here if you see Slash find now instead of name I am giving like this slash or 1 = to 1 now directly it will replace here so what it will be like user undor name equals to so first this will finish this then it will append this or 1 = to 1 and then you have this one which will complete this now your query become this select estess from user details where username equals to this so it is empty or 1al to 1 become true so it will return all the values in the DB you you got it like what happened here I put certain values which is not properly uh you can say that validated and used directly into the query and I manipulated it such that it returns me the complete data or unauthorized data we can also use it to F the DB name table name and you can also drop the D DB also tables also so it's very very important uh to get protected from the SQL injection and how to protect it by we have already seen that in the SQL when we covered jpql right by parameterized query we can protect it so whatever the name is coming don't use it as it is you have to use like this parameterize it okay so set parameters so that it would be like not as considered as a query it will be considered as a value string okay and then you will get no data so because with this name/ all 1al to 1 nothing matched so it's safe now okay guys any doubt let me know uh we'll discuss more thank you bye
