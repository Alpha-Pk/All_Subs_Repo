=== XSS (Cross-Site Scripting) (575s - 1000s) ===

scripting very very important okay understand it from a use case where you have a web page where considered as a comments okay user one its comments user to its comments now there is something called post you have to post something okay and once you post it it will also be shown here like I have posted let's say my commment it will come so for now what xss does is it allow a attacker to put a malous script into a web page viewed by other user so now let's say that this is the web page if you come you also see the comments from different users and you can also post your comments also so what if you inside a post instead of writing you put a script some and then a script would be stored now any other user comes to this web page it comments would be shown by user one user two and also your comment where you have put a script so that script will run so commonly used for steing the session or deform the website okay I will tell you how we will see a demo so for demo purpose I am creating two endpoints one is a get Call of this/ exes where it will what it will do is it will just load all the comments which are present just return it nothing else okay and also you will see that I am using controller not rest controller okay why because uh I want to render the page I want to render the page I will see you I will show you with an example what does it mean the second API is post SL commment what here what it will do is this you are allowed to post anything and that will stored it in somewhere in the DB or somewhere in memory and during get call that whole complete list would be shown very simple and this is a code I have written very basic so I have used at theate controller and if you have following the spring boot playlist you know that what is the difference between controller and dress controller okay since I am using controller I'm returning xss it will try to load xs. HML it will try to render it okay so there I have created one xs. HTML okay so now here if you see this get end point is like just whenever this is called it will just simply return all the comments all the comments right whatever we have and in the post I am just adding a comment whatever the comment I am coming see whatever the comments I'm getting in a string directly whatever the user is putting some script or something I directly added into my list okay since in the spring boot everything is a single turn only one object is created so that's where even if you hit multiple times it will get stored into this objects only okay okay so don't get confused why why not creating every time new object so anytime you add a new comment it will keep on appending to this list okay and during get it will simply return all the list okay so now let's see step one I am hitting slash xss okay so here it will render this page here I am rendering this page leave a comment and simply you are able to see whatever the script you want to put or comment you want to put you can see submit okay and all the comments will be shown here so let's see okay so here if you see that leave a comment now let's see I am an attacker and I have put script alert xss attack some any just basic script now once I call This And click on submit what would happen see it's a attack happen there is a popup comes why because your browser initiated the script itself your browser initiated the script itself so when you click on submit you first click the post API this is the post end point so means this comment got stored this comment got stored now let's say any other any other user is coming and to this web page so definitely there is a get call happen SL xss okay and it will load all the comments and as soon as this scripted comment get loaded your browser will execute it and this uh popup comes now you will say that hey what's the problem in this so it's not uh uh like harmful right it's just a popup just assume that from this a simple script what if I change to this one I am taking you to some attacker site and just say that this is the attacker link URI you don't even know and in the request I just put document. cookie you know that now whatever the cookie is present in your browser will get to the attacker now attacker knows your cookie and this cookie only has your session ID now wherever you are authenticated where this session ID is applicable attacker can access it freely totally so it is not just like one popup attack you can replace this inside in instead of simple alert you can change it to like this by for stealing your cookie now attacker has your cookie your session you will lose the data or the security compromise so what shall we do to protect it so generally we have to do proper escaping the user input so wherever we are accepting the user input we don't know what user is entering so we have to take this special characters and we have to properly Escape it like this has to be changed to this okay so that browser don't treat it as any script which it has to run so it will be shown as it is like an string so you don't have to worry about that your any script will run okay and you have to do proper validation what what values are allowed what values are not allowed in the like user can use so ex is clear right there is something called C also
