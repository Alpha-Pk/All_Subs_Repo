=== Flow Diagram of ExceptionResolvers involved in exception handling (67s - 1015s) ===

when an exception occurs and the role of each five classes so now let's say that you already know that the first controller which spring boot provide to us is nothing but a dispatcher subet so dispatcher subet is the one which actually invokes our controller also so it is is possible that our controller might throw an exception or dispatcher not able to invoke our controller itself so in any case let's say any case exception occurs what would happen so dispatcher subet will pass the request to this Handler exception resolver composite class okay so this is this class Handler exception resolver composite class so this class I considered as mostly an Helper and most probably you can say that orchestrator orchestrator of what orchestrator of this resolver so there are three resolvers exception Handler exception resolver class second is response status exception resolver and third is default Handler exception resolver this three these three are resolver so here this class Handler exception resolver composite class invoke this resolver in sequence first will be invoked this second this and third this so here what happen is whenever an exception happens so this exception is passed to the first resolver this is your first resolver exception Handler exception resolver so once this resolver process its work the control goes back to your composite class then it check hey exception is handled if yes if this resolver is able to handle your exception then it will not grow further it will return to this one default error attribute class but now let's say that if the handle if the exception is not handled then this composite class will pass it to the your second resolver which is response status exception resolver and after that again before proceeding to the next resolver it again check hey is your previous resolver is able to handle the exception if yes then you know that it will goes to this step default error attribute if no then only it will goes to the third and last resolver default Handler exception resolver okay and once all the three resolver complete its task ultimately the control again comes to default error attribute so no matter whether any resolver is able to handle it or not ultimately the control will have to become come here if they are able to handle it then directly will come it will not go to further resolver otherwise it will pass through all the resolver one resolver two and finally resolver three after that again the control comes to this so and after this the response has been send to the client so at this point of time you would be clear that these three are the resolvers classes which actually handles an exception it is followed in sequence this is First Response status exception resolver is second and default is third and this two are the helper classes this is nothing but an orchestrator which actually orchestrate this three resolver classes and after that this is the last one which actually create a response with proper status time stem what is the message proper error response and ultimately send the response so this is ultimately where we create a response actually what is your status what is error message status let's say 400 error message is let's say bad request something and ultimately the response is sent okay let's see a flow with an example this will clarify you more so I have created a very simple controller class okay I have one rest controller class get mapping means it is/ API SL get hyphen user API I'm creating it and I'm throwing null pointer exception and throwing one message throwing null pointer exception for testing if you run this what output you will see I am hitting this API what output I will see 500 error code and here time stamp status 500 error internal server error and path how who set all these things I I never said here that it's a 500 internal error code but somewhere internally it has been set who we will see that just one more example creating a one more controller but here now I am throwing new custom exception okay you in Java I have already explained you how you can create a your custom exception so I created a custom exception I extended runtime exception class okay and I have just exposed two Fields here status and message in the Constructor I'm setting this field and the getter method okay so here if you see that I am throwing new custom exception setting this let it's a bad request and this is the message request is not correct user ID is missing so I'm setting a status and message and I'm throwing this now if you run this hit this API what output you are seeing again 500 internal server error the time stem current time stem let's say status error path now here if you see the 500 internal server error now if you see that hey you are returning bad request which is nothing but a 400 and messages request is not correct user ID is missing but why this seems 500 internal server error is coming what is happening here one thing you need to consider that in both the example we are not creating response entity object we are just returning the exception so some other class is creating the response entity object so from the previous video you already know that whenever we send a response we have to create a response entity object okay in that we set two things body and status but here if you see that we are just returning an exception but it's still in an output we are seeing this response so somebody is creating response entity for us who I think you already know the answer right who it's actually default error attribute but why 500 why 500 so here what happening is when you are throwing an exception and not returning response entity means you are utilizing the exception resolver framework now your all the exception will pass through this flow okay if none of the resolver is able to handle your exception then this default error attribute by default value it will set 500 okay so 500 is nothing but a default status code okay but if you want full control and don't want to rely on exception resolver then we have to create the response entity object always remember and here the example which I showing you many Company still follows this they don't rely on exception resolver so what happened is here the same thing I am throwing custom exception status and message very basic okay this is your custom exception class and just uh just to recap you this message is just uh optional you can even send super super message so this runtime exception its parent is exception class this has I think one field called detailed message okay so this detailed message is somewhere which uh the framework wanted this message is ultimately get set to this one so either two way either you just do super message okay and ultimately it will go to runtime exception runtime exception here if you see uh let's go runtime exception here again it says super message and here ultimately it gets set into detailed message okay this is one way or if I don't want to do super just for clarity purpose I just added here I just added a string message setting it and override the get message itself and returning this message here itself okay instead of uh if you don't want to override it then you have to do super and pass the message and UL Ely this message will get set to that uh detailed message field this one and then if framework call get message this one this detailed message would be return but if you don't want it I already told you that uh you can override this one you can override it and return this message okay so this is just a quick recap like uh so now come back to this one I am doing a try throwing an exception sending an status and message now I am catching one I am catching this custom exception and another I am catching this generic exception so definitely this custom exception will get caught this throw new exception now here if you see I am returning the response entity I am creating the response entity body and your status so body I am sending is I'm creating an object object which has this error response is just nothing but a pojo class which has three things time stem message and Status very simple so I'm creating a new object which is nothing but a pojo I'm setting the date current date what is the message so whatever the exception is coming okay I'm doing e. get message so message would be said and whatever the status code E.G status okay so this status would be set into this object and this object I'm putting into the body and then the response what is the response status I said this e doget status this one only so this is how your response going to look like the 400 bed request which is response entity status and this is your body time stem status and message this one time stem status and message so now here if you see that this resolver I told right three resolvers are there those are not even involved now you have taken control how you are going to handle an exception custom exception happens how you want to handle it so you have created a response entity and it got return properly you are able to see proper error messages which you wanted to return exception resolver is not invoked but when we don't return response entity we just simply return throw somebody has to create response entity for us that's where the framework comes into the picture and that's where this all resolver and flow comes into the picture now I already told you now when you throw custom exception so this composite will pass this custom exception to this class resolver the first exception Handler ex exception Handler exception resolver says that hey I only able to handle at theate controller advice and at theate exception Handler annotation apart from this I don't know so does custom exception has any such annotation no so this say that I can't handle it it goes here does exception handled by previous resolver no so it goes to the response status exception resolver hey can you handle this custom exception it says that hey I only take care of at theate response status annotation I don't know about anything else again it goes here hey does this resolve it does able to handle this custom exception no then it goes to the default one if you go to the default one default one is very like here if you see default Handler exception disolver class it has very predefined that okay it handle spring framework related issues like method not found media type not supported right missing parameter exception so it only able to handle predefined exceptions so will it understand custom exception no I don't understand this and ultimately all three resolver pass and control comes to here and default error attribute what it will do here if you see default error attribute it will pass it will put time stem current status now nobody able to handle it so default status would be set as 500 and all other details would be set path error details and it will return the response entity the response entity would be created with this object and return if any of the resolver is able to handle the exception then they will just set status and message okay now let's say that just assume that exception Handler exception resolver is able to handle the custom exception let's say it will just send that okay for this exception this is the status message let's say 400 this is the message it will just set this and then it will say that yes handle it will close here it will not proceed further and default error attribute will set proper status not default one 400 proper message and then return it so you got the flow so here if you see that default error attribute set this attribute time stamp status everything and then once the attribute is created it will create a response entity with body and status and return okay so I'll show you let's see now let's say this is an example where I am
