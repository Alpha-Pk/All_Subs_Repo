=== Demo of ExceptionResolvers involved in exception handling (1015s - 1194s) ===

having a controller same very basic I'm throwing a custom exception bad request user ID is missing see I am not returning response entity I'm just throwing it directly if I run it let me debug it I started the debuger point now let's say we are hitting it get user okay where the control comes composite first now here in the composite you will see the resolver it will iterating it it will itting it all the resolvers so now here if you see resolvers three resolver first is exception second is response status third is default one okay so if we assume the this custom exception will goes to now your exception Handler exception resolver the first Handler the first resolver it will not be able to resolve it so it will goes to the second resolver which is response status exception resolver response status exception resolver is also not able to handle it then it will goes to the third one your default handle exception resolver so in the default one no one would be able to handle it ultimately it goes to the default error attribute and what is the status it will send so here if you see in the attributes the status it will set to 500 internal server error because no resolver is able to handle it no resolver is able to handle it and ultimately you see 500 internal server message so now question is what exception does this three resolver handles okay we know the flow we know the sequence we know the reason if we throw an exception if it is not handled I will get 500 default value but what this resolver handles actually I don't want to use custom let's say you don't want to use this you don't want to handle in each you know that there are so many methods there are so many controllers do in each class in each controller in each method you want to do try catch and handle the exception if it is a small application it yes it's good but if it's a huge application it's mostly you will find it's a code duplicate for the same custom exception you have to send a status error message and different different places so we can take use of the framework but before that we need to understand what this three resolver actually handles let's start with the first one
