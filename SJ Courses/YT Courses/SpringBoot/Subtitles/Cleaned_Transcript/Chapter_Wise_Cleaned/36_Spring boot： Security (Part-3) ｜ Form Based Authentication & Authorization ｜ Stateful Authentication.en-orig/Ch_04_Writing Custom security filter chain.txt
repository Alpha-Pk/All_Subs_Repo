=== Writing Custom security filter chain (2250s - 2515s) ===

okay sh let's say that if I want to change few things like I don't want this default login page or default logout page I want to create mine mine login page M log out page or I want to relax authentication on few endpoints if you see by default in the framework what it says that no matter any request you have to be authenticated but let's say that you have like hey sh I have certain public API I have some public apis for which authentication is not required I don't want to force the user to authenticate these are public API how can I relax them simple write your own security filter chain very simple right so in my configuration at theate configuration I have created my own configuration so here if you see that I have said the same thing any request authenticated but any request matches this any API let's say/ users API permit all no need to authenticate it so I am considering this is my public API this is just for example right so let's say that/ user is your public API you don't want any authentication so I just put do permit all and find any other request authenticate and here if you see I have just used form login with defaults so customized what with default so here with default itself login page is your default SL logout is your default so if you want your custom login page you can change here like dot form login you can change something like form so something form. login what is your login page something my hyen login something like this you can change it whatever you want to do but what I mean to say that you have to write your own security filter chain right so now you get to know at this point of time let's say that if you have to work with JWT you will always see that we have to write this security filter change because we don't have to use form login we have to tell something like hey something JWT something because form login is the default one otherwise we have to tell it so now you know that okay why for in my company we are using form but we are not writing this Bean why because it's already the by default one okay now form based authentication is clear right but still one thing left is authorization filter and also one more thing I am not going into like we have seen that right by default uh the spring creates the table for you that uh session session tables uh this session tables right where it gone yeah spring underscore session and also there is one more table right spring undor session attribute something so these two tables it creates where it has all the data present we can also do that we can instead of relying on Spring to create table for us we can override and we can create our own table I don't want to go into that way because uh this is I don't think that it is widely used now but this is to set a fundamental in 20156 when I was working we were writing our own tables and we were managing those table like we have more tables and we are managing the session whether to increase the time or not okay whether to uh update this column or not like all this stuff but I don't want to go in that way like where we are managing by ourselves just consider that it is possible otherwise if you don't require it you have the basic need let the spring manage your tables spring tables for you and you already know that why it is required because you have different servers then you need to store somewhere otherwise request goes to different server if it is stickiness is not maintained then there you will ask unnecessary user to authenticate okay yeah now let's come back again to
