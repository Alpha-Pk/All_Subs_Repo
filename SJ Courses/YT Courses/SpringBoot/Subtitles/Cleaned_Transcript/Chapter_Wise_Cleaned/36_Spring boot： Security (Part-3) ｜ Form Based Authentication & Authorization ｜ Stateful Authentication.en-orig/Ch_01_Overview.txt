=== Overview (0s - 298s) ===

hey guys sh this side welcome to concept and coding and today let's start with our first authentication method that is form login authentication so uh in the previous video we have already seen how we can create a user dynamically and what is the industry standard so this is the very base of you can say that the authentication method before we I know that we eagerly wait that okay let's why not directly jump to JWT which is very very frequently used nowadays but to understand JWT also we need to First understand what was previously popular what were the disadvantages how it was working and then only while understanding and learning JWT and O it would give us a better sense okay what advantage they are adding okay so let's not skip this and let's understand this like it would be a uh like very good interesting uh you would get to know a lot of things in 2015 I used to work a lot in this but now yes uh it may be very used limited purpose okay so let's start without wasting any further time so form login authentication it's a stateful authentication method stateful means your server is maintaining a state of a client okay server maintains a user authentication State AKA also Al known as session so server maintains the session so that user don't have to provide username password every time with each request so in this what we have happened it you have a client or let's say that user and you have a server so first time only it provide username and password at this point of time Server create a session and stores it okay now also send this session back hey this is your session now for all the subsequent request the client do not provide username and password it just provide the session it just provide the session and server validate the session if it is validate the request has been processed successfully if the session is not valid whatever the session ID is provided by the client if it is not valid or not found the request is not entered okay so that's what it means say so that user do not have to provide username password with each request for with each request they provide a session so user enters their credential in an HTML login form this is for the first time on successful authentication a session is created to maintain the user authentication State across different requests okay so server is the one who creates a session and and stores it okay and also in the response it sent now with subsequent request client passes this session ID not username and password very important it do not pass username and uh do not pass username and password it only passes the session ID and server validates it with the session ID which is stored internally okay so I'll tell you we will see with an example and also see this one why it is important it it is a default authentication method of springboard security so if anybody asks you like uh what is the default authentication method is it jwd no still form based authentication is the default authentication method so if you have to use a JWT we have to manually tell hey use JWT otherwise if we don't tell the springbot considered it as a form authentication method I will tell you where it is uh defined as like it is a default also there are default URL of a login and log out so login URL is/ login SL logout so what is this like so the first URL where we have to provide username and password right so it has a default URL is login okay and once your session is created once your session is created okay it is valid for a particular amount of time we will see that but let's say that you have to invalidate the session before that time you can call log out/ logout so these are the default URL which are provided by Spring boot for the form based login you can use this when you use this login you can provide username password your session would be created and if you call log out your session would get invalidated okay we'll see that so this is the default login URL log out URL now one first thing comes to mind
