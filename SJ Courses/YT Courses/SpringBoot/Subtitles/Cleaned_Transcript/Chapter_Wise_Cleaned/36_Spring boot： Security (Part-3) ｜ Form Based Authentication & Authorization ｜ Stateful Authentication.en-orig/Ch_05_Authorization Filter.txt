=== Authorization Filter (2515s - 3017s) ===

authorization filter which is left so as I already told you that once the user is authenticated when user is trying to access any resource authorization check is mandatory it is done to make sure that user has the permission to access it we already seen that by default spring boot security do not put any restriction again do not put any restriction on any resource we have to do it manually now let's say that you have endpoint sluser users by default if user is authenticated if user one try to access it authenticated it will access it user two authenticate and it try to access it it will let it exit it don't put any restriction by default we have to do it manually that oh hey for user one look for this role whether this should be user role for user uh to look for this role admin role it should have while accessing this resource so we have to tell that if you don't tell that no restriction if you are authenticated go and access the resource so authorization has two phases phase one so phase one is like you know that we have security chain filters security chain okay after that security chain filter chain what is that your dispatcher sulet and then there is your controller ultimately okay so authorization done at two places first at filter filter level second at controller level authorization check as part of security filter that's where our authorization filters comes into the picture right and the second phase is authorization check after request passes the security filter but reaches the controller so request passes the security filter but reach till the controller now there I'm checking that user is has the authority to access the resource or not so we will see this later because currently we are looking at the filter okay so this one how to do at the controller level authorization we will see later because that is common for form based basic JWT or any other authentication method because the control now re still controller now there we have to do an authorization so that part is common for all the authentication method but this part security filter this is uh unique for each one so for form base we know that authorization filter comes into the picture we have already seen that so how it does that so if you look that it's very simple I told you that by default spring boot security do not restrict any resource we have to do it manually so I'm doing it manually in the security filter chain I am telling any request matches this/ users should have the role has Ro user this is just a string you can give anything okay and and internally it append r l roore so it append this so ultimately internally it would become role user like you can give anything it's a string but maintain uh like follow some uh proper naming conventions okay so what I'm telling is any request matchers like/ users should have the role user okay so it it will authenticate also it's not like it will not authenticate authenticate plus has role also will be checked also here in the application. property I have added while creation of a user and password I have also assigned this user or role user okay we have already seen that like in the dynamic also how we can while creating a user how we can assign a role so while the application is start up user is created let's say there is some user table user is created with usern name password and the role user is created okay and we have mentioned that/ user has the role user it required this now what would happen now I am manually restricting that any user trying to access this endpoint should have this role okay and also I told you no need to add Ro underscore it will automatically get added now in not authentication filter it is authorization filter now in authorization filter it will validate okay before I say anything just a very quick recap when the authorization filter is invoked what is passed to it security context holder what security context holder holds security context holder hold security context data which has all authenticated data authentication data is nothing but this this this is authenticated data right so it has all the user role everything which is there in the DB only from DB it found right it call it fetches the user detail from the DB username password role it has all this data all right so now when authorization filter invoke it will validate okay user is trying to access this user's endpoint and this endpoint required role user roore user this because we have tell here and it knows that authorization filter got security context this has authentication data where all the user details is present all the username password role is present so it knows that this user has what Ro Ro so we have already given this role okay so it will match if it match well and good it will let it go through the controller if not it will pass forbidden exception forbidden this one and in application. properties let's say I am giving that this user has admin role but not uh but this API required this user role now now let's start the to let's first try to user login user pass user is successfully login now I'm trying to log in user AP endpoint when I hit it says that forbidden 403 there is a mismatch in the role and authorization filter is the one which throws the Forbidden exception also we can give as many role to uh user like more than one okay so here if you see request matchers this API if anybody has now I'm using has any role now I can provide like a list user add anything and similarly I have provided here like this by comma separated I can provide as many roles I required okay so I hope by this point of time form login authentication is clear so there are few questions can come
