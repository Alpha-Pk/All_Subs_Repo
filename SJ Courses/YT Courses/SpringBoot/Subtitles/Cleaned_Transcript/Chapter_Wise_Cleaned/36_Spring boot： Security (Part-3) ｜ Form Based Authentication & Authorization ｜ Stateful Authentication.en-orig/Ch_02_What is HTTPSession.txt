=== What is HTTPSession (298s - 860s) ===

that hey what is session and when does it create it so this is one basic uh flow i showing you this is your client could be your browser and this is your server the first endpoint they hit is login SL login which is also a default one okay with this one they will provide username and password okay so when this happen what server will do it will create a new HTTP session so this is one object it creates a new object this object is HTTP session object okay it it contains various information like what is the session ID okay when the session was created it's expiry time okay Etc there are a lot more information we will see that so it so it creates a HTTP session and provide one session ID and saves it in the subet okay we can also save it in the DB also but by default you can say that it's in memory like uh it's in the subet itself like Tom if you're using tomit it's inside a you can say that tomit or you can say in memory RAM so this object is a stored created and is stored in subet also now once the authentication happen username password authentication happens sulet uh session object is created the response in the response in the return it set the cookie also in the cookie the J session ID it will put the ID this session ID okay so it returns that now your session is now on okay currently it is active State now for any further request from the client now they are not passing you username password okay they might be calling slash users or slash anything okay they are just passing in the cookie session ID and what is the session ID is the one which was received previously now once it get server get it what it will do is it will search for HTTP session object with the ID this one if there is an active session it will find it yes it fetches it okay if it is f if the HTTP session object is found and if it is valid it is not expired it will fulfill the request it will entertain the request whichever it is coming but if the HTTP session object is not found or it is not valid already expired it will redirect it to login page it will red directed to the login page okay so that it has to reauthenticate themsel so this is generally known as when we say that session you this is known as yeah a session has been created means it is maintaining the user authentication State yeah user is still valid uh any request which comes in yeah uh we can fulfill that request okay so by default time to live for the HTTP session is 30 minutes so this object which created right HTTP session object by default it is 30 minutes but depends upon sulet right if it is a tomcat subet container you are using then it is 30 minutes but I would say that it depend upon the subet container which one we are using also we can configure it too if if you don't want 30 minutes you want like okay I want only 5 minutes we can change that also we'll see that and we can also store the HTTP session in DB also so currently I told you that by default it will store in memory right but we can also make sure that it can store in the DB because it is possible that we have many server server one server two okay if they are restoring in memory the session and if this is a client this is your client if the client request goes here yeah session object is present in memory it would be able to fulfill the request but what if the next time client request goes to the server two now server two don't have the session okay then it will ask client to reauthenticate hey first login okay so it would be a bad user experience so generally in that case in DB it has been this session has been stored in DB this session has to be stored in DB so that no matter client request goes to whichever server from there they can query the DB F the session whether it's valid or not and fulfill the request based on that okay so see this first in application. properties I am setting server dolet do session. timeout so this is you can change this one I have changed it to 1 minute okay by by default is 13 minute and I change it to 1 minute now if there is no activity from the user site for 1 minute then in 1 minute this session object will get expired just see what I'm saying no activity so only after 1 minute of inactivity makes the session expired but now let's say It's 121 okay now you are expecting that by 1202 my HTTP session will get expired if there is no activity but let's say that user made a request at somewhere in mid okay so this expired time will increase okay so it required a 1 minute of inactivity total inactivity so this would when when it was last used when the last activity happened from there it will again count okay inactivity of 1 minute then only HTTP session will get expired so that's what what I have written if user activity keep on happening it will keep on re authenticating and after 1 minute session will not get expired internally what it will do is when I say that it will keep on re authentica mean internally it will keep on increasing the expiry time okay and second I told you that we can store the s TP session in DB so it's a very very simple way if you see that we need this dependency which I told you that u in the first architecture video also I told you uh for the Spring Security like we can have this session spring session jdbc in the form. XML and in application. properties we can provide this three configuration this configuration spring. session. JBC do initialize hyph schema equals to always means we are telling spring boot that hey you create me the table you create the session table for me I don't want to create table myself you go ahead and create a table so here if you see spring boot will automatically create a table for me Spring undor session table for me okay and I have told sulet do session. timeout 5 minutes so after 5 minutes of inactivity my HTTP session should get expired okay and this will like um just tell that we are going to store the in the instead of in memory we are using DB so here if you see that this is the table which springboard will create this is primary ID this is the session ID which was created what when this session created last excess time maximum inactive interval 300 expiry time so based on the last excess time it will just keep on ADD 300 and it will keep updating the expiry time okay and this is principal name is nothing but a username so this expiry time will keep on increasing if user keep on sending requests based on the last exess time okay so here if you see that my session is now stored into the DB instead of inmemory so at this point of time I hope that at least we are clear what is form login authentication okay and when we are saying it is a stateful authentication means it is maintaining a session and what is session actually okay so since we are clear with what is session now let's understand the
