=== What are Session Creation policies? (3109s - 3250s) ===

is session creation policy we haven't discussed anything like that right but if you see the that do session management do session creation policy is Riff required even if we don't write this this is by default added if required so what is this session creation policies so there are four values it can take if required always never stateless if required means HTTP session is only created when needed and this is the default that's why above we haven't used it what it mean if only required so let's say that I have a public API I have a public API okay for that one HTTP session will not be created okay so it only created for which it is required wherever authentication is required it create HTP session for that only but if you change it and use always always then no matter what HTTP session is always created if already HTTP session is present then it will reuse it but no matter whether it's a public API or not a public API STP session would get created okay but generally for public apis authentication is not required so generally you don't require you don't need to maintain the state because let's say public API is hit by uh millions of time you don't have to maintain an authentication it's just like informational okay so that's why if required is generally preferred and it default you don't need to create a session for each apis also for whichever it is not required never says that do not create a session always use if present okay and Stat less is like no session is created at all all it is used for stateless application like JWT okay so this is what session creation policy mean and this is how generally we set and the last question which can come
