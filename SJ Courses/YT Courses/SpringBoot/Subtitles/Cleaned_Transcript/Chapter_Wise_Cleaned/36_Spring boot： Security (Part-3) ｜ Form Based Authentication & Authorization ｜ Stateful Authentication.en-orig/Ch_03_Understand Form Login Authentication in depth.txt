=== Understand Form Login Authentication in depth (860s - 2250s) ===

flow diagram of form based authentication method very very important and also by this point of time you get to know that it involveed two step first step is login where user provid a username password okay and through that we create a session second is request like accessing the resource or you can say that request or accessing the resource where whatever the session comes it will add the session and at this point of time it will validate the session and fulfill the request okay so now let's understand end to end flow for this form based authentication method and let's see that if we have to implement it what is required do we need to do anything so first step currently there is no session active user is loging for the first time means session do not exist at this point of time only user exist means and also you already know that in the previous video I've already told that how to create user okay dynamically also for in for testing the form based authentication I have created a static one but you know that how to create Dynamic one uh but user has to be there so assume that we have already present user we have username password already present user exist now what the first step user are trying to do is calling the API SL login this is the default login page form login page which provide okay we don't have to code for this one spring boot automatically provides right uh for the form based authentication so when you hit this API it will ask for what is the username what is the password okay so we already know that what is the username and password we already have seen the previous video I have given I have created this is my user and this is the password password and I clicked sign in so here if you see that username goes password goes and csrf token gos okay so now what happened in the back what happened when this submit has happened so now call comes to one of the security filter CH as soon as you click sign in okay this username password comes to this security filter chain now see understand slowly what would happen this is the chain security filter chain and this is the first filter which will get invoked username password authentication filter okay so now here the flows happen so this is the first step which it will do it will create and pass the authentication object to authentic ation manager so it will create an authentication object authentication object is nothing but this object username password authentication token it is nothing but a child of authentication class authentication class so it is just a child of it so we are creating an authentication object so it has your principle which is nothing but a username credential right it is protected currently authenticated is false it is not not yet authenticated and no no other details present only this much username password that's what we are getting currently from the request now it passed to the authentication manager and we know that in the architecture video I've already told you this authentication manager delegate the request to one of the provider so it will delegate it to Dow authentication provider okay now what it will do So currently we have username and password coming so this password if you see currently it's not hashed form so it first call password encoder okay and encode or hash this password whatever the incoming incoming raw password is hashed first okay so that is done after that what it does it it will fet the user details either from in memory or DB we have already seen in the previous video what's the logic behind it how it is done okay so it fetch the user detail we have a username right based on the username it will fetch the user details so user is already created so it will get what is the username what is the stored hashed password what are the roles user has so all this information it will get after that it will do it will validate the incoming username and uh password okay and this password it has also done the hash okay so it will validate the incoming username and password with the stored username and password with this whatever the it was stored in previously user already created right so whatever the user is already stored its username and password and whatever the incoming username password it matches it if it matched then it will update the the authentication object what all things it will update it already has the username it already has the password but password it don't add it it just a protected we don't put it now it change if the matching happen then it says that authentication is true yeah it is authenticated also the role whatever the role so during the user creation we might have provided the roles which user has we have already seen that so that Ro also it will put this role is a for this uh user it has these many roles okay so it will update the authentication object and it will return this Au uh fully authenticated object return back to this filter till now no session is created okay only what happen is we have an authentication object which has all this information now which has roles authenticated true username and yeah all this information like account is not expired it is enabled credentials is not expired all this information is present now what will happen now the next filter it invoked is security context holder filter what it will do what it will do is it will first create a security context object and put that authentication object inside it okay so now you can say that this security context has all this information actually okay so this security context has all this information and then it will pass this security context object to http session security context repository class what it does it will create a new HTTP session object a new new HTTP session object is created and it stored the security context object inside the HTTP session okay so this HTTP session has all this information now okay so you will get to know that when this will be helpful why it does that so first this authentication object it stores into security context and this security context it pass to this HTTP session security repository it creates a new HTTP session object it could be uh like stored here in the DB or in memory okay and this currently it is empty and then later on it whatever the security context is there it saves in inside a HTTP session object so HTTP session has uh all the info you can say that uh like session ID and you can say that all the expiry time creation time and also it will hold all the uh security context info like all this information okay so I'll show you here if you see okay I'm just starting a server I'm just uh showing you I am just doing login so I have already created cre a default username and password us e r and password I have created P SS I have in the application. properties itself I have defined the default username and password from the previous video you already know that I have done sign in okay user is sign in so means when I have done this what would have happened so means it has completed all the flow so now let me check the DB so spring set session so this is the session data session ID creation time last exess time maximum inactive expiry time and this there is something called Spring session attribute okay so if I see what is the Spring Security context and this is the serialized data this is nothing but the security context data this one this all data is actually stored in the session right so this is all part of session data spring session attributes okay so HTTP session object is created and the whole session uh security context object is stored inside a HTTP session and it is stored either in memory or DB okay so till this point it is clear what has exactly happened now after successful authentication if login endpoint was used if login endpoint was used because you know that after security filter chain it goes request goes to dispatcher subet from dispatcher subet if there is Interceptor it will go there otherwise goes to controller means some endpoint has to be invoke right so if you are using hash login endpoint so login is just for uh spring boot form login authentication if it is used then it will hit the default endpoint like just slash okay so like this I have first open this URL SL login I have provided my username I have provided my password I have clicked on sign in and when I clicked on sign in the everything happened okay uh how it goes and fetches the user ex existing user it compare whatever the username password I have provided then it uh whatever the security uh context came authentication object it put into security context then it created a HTTP session object okay and this security context also put into HTTP session and since I am using DB so all the details for the HTTP session right session ID expiry create time and also this uh this HTTP session has the context right the security context detail that is also stored there in the DB and after that it has to call the controller so it calls the default endpoint this is nothing but a slash so I have created one get mapping of this default endpoint I just say hello so that's why whenever click sign in it will always print hello Okay so this first flow is now completed we know that how the first flow is happened now the session is created and we know that the session is created for currently in our case like if we check the DB I showed you right by default is 30 minutes but in DB for this one I have showed you it is for 300 like 5 minutes okay because that's what I have provided into application. properties 5 minutes okay now yeah before that again like if you are not using slash login if you are using directly SL users so now what would happen is it will try to look for that okay you are trying to access a record uh resource is it already authenticated no so it will redirect me to a login page where I provided a username password the same thing happened in the background HTTP session created properly okay and all the session details either in memory or DB and after that is done it has to invoke the API so Now default API is not invoked because I was trying to access slash users so it will call the same only SL users only okay so default is only default endpoint is only get invoked when you are first trying to hit SL login okay now the second point so we have completed till this one your client and server the client hits server with username password server has created the session HTTP session is created with ID and it has all the session details uh also plus security or you can say that security context data which has authentication authentication details so it has has all this data either in DB or in memory and it has return the uh this ID session ID in response till that we are done it is set into the cookies now after authentication user is invoking any subsequent apis let's say I'm trying to hit slash users or slash any other resource in the request I'm just providing the cookie and this session ID this session ID ID now what would happen how it will validate it so again the first part is filter chain will come into the picture now what is what are the filter chains the first filter chain we get invoked is security context holder filter okay so now this is the so now here if you see username password authentication filter is now not involved okay security context holder filter will get involved and and what it will do it will first pass the request to http session security context repository it will try to F the HTTP session based on the J session ID in the request now any subsequent request in the cookie they are passing the session ID so it try to F the HTTP session whether from inmemory or from DB it will try to fetch it if HTTP session is not valid or not found okay it will redirect it to/ login but if it is found what it will do is it will get the security context from it you know that right in the HTTP session object what we have put above in the HTTP session object we have stores the security context and security context has uh nothing but a authentication of data so it fetches the security context from the HTTP session and Returns the security context okay so what it will do is now it will store the security context data you can say that the authentication data generally in one placeholder security context holder okay so this security context holder is available in the complete life cycle of the request till the point of you can say that uh your controller get invol this security context D holder data is present so whatever the security context is coming from the HTTP session it has all the authentication data it has all the authentication data okay it will just put into the security context one placeholder holder and that is now available throughout the life cycle of the request so first thing done as soon as the request come it Ables to it go and find the HTTP session and it take out the security context okay now it will pass it to the next authorization filter I will also tell you how authorization works but in the authorization filter it check does user has the permission to access the resource or not let's say that user is trying to hit slash users API yes user is authenticated from this J session ID I am able to find the active HTTP session and it is not yet expired I take out the authentication data okay so that my complete life cycle in the controller let's say some flow needed some data they can fetch it but I have the authentication data but authorization filter is also required whether this user let's say that this user one has the permission to access the slash users resource so that authorization filter validate I will show you how and once it is validated yes it has the permission now the control will goes to dispatcher solet particular controller now it will access the request follow the fulfill the request so you got it like this two step so if you just see now you will say that okay now I have to implement the form login authentication what I have to do so if you just see we don't have to write a single line of code everything is handled by a framework itself everything which I told you like this filter chain everything uh like it is fetching the HTTP session right authorization filters everything is already there in the framework okay we don't have to do a single line of code all we have to do is configuration okay I'll will show you and also I told you right it is the default authentication method of a spring board so if you open this class this is a framework class spring boot web security configuration if you see that default security filter chain in this one form login it has set okay so there are different so it has two default like it has used form login and basic authentication it has said both okay but form login it has set it so we don't that's why we don't need any code even it has set like okay even if you don't tell anything by default it will know that yes by default user might be uh using form login only so all the it the flow which I have told you automatically it will go through that flow only so all I have added is the dependency and the config so dependency is that this is the security dependency we already know that like till now and this dependency is like I want to store my session into the DB that's all and in the application. properties from the previous video you already know that I have uh created a username and password like hardcoded one I don't want to get a random password every time okay so from in the previous video I've already showed you what are the different ways to create a username password okay and this is the hardcoded one is just for the testing purpose it's not recommended in the live and the live recommendation I've already shown in the previous video so I have created a username password so when the user uh when during the server application startup time one user would be created with this user and with this password okay and this is the second thing which I have told like I want to store the session into the DB and I want spring boot to create the tables for me and the session time out I want 5 minutes okay so we can test this one like if you hit the API SL login API you will provide username and the password P SS okay now if you click on the sign in okay so you will see that in the response after the successful authentication like uh in the response set cookie you will get a session a session has been sent to you okay and later on any API you will is okay you will see that in the request we are passing that session ID again okay so this is in the request we are passing it to the server so we have done nothing all we have added is form and application. properties and this all has been handled we are able to do all of this stuff okay now one thing comes to your mind that
