=== Disadvantages of Form based authentication or Stateful system (3250s - 3481s) ===

in like what are the disadvantage of form based authentication I think by this point of time you already got it like first is like as we have discussed in the previous vulnerable to security issues like csrf we have already discussed very beginning all the attacks with demo okay how the csrf demo session hijacking is possible so form based authentication is vulnerable to it but if you see that by default csrf is enabled for form based login you can disable it also in the security filter change you can say that csrf disabled but we should not do this for form based authentication always unable it csrf right because it is vulnerable to attack like csrf so do not disable it this is just for sample I'm showing you you can disable it and session hijacking so you know that the session ID is passed in the cookie so it is uh very much likely that your session get hijacked it got is stolen and using this your J session ID the attacker uses it your resources access your resources the second is the session management is a big overhead So currently everything we have seen is handled to framework but I told you that when in 2015 and 16 we were working in a company it was handled all manually like because as per the business lot of things in the session we have to uh manage according to business need right so generally if it is a very simple business then yeah you can use framework but definitely as the business grows lot of custom needs required so you have to manage the session yourself so session management is a big overhead you have to have like session tables you have to manage its expiry and all this stuff right and also now in case of distributed system like it would be like a SC scalability issue for sure and this is the subsequent issue is that database load now you have in the distributed system let's say you have multiple services or let's say that even for one service you have multiple instances definitely you need a DB then like you can't have uh session stored in memory what if as I told because nowadays we have load balancer in front of the client it can pass the request to anybody so if earlier session is created here user says that you are authenticated now the second time it try to access the control goes to the second instance now you say that hey where is the the session or and it will say that hey reauthenticate SL login page again so this is a bad user experience okay so that's why in memory is not used if you have multiple instances you have to put into DB so that all the instances F the session from the DB so there are multiple servers then we need to store in the session into DB or Cache so again memory you required lookup time is required which increases the latency okay I hope this form login authentication is totally clear right do practice it with your hand even though it is not very frequently used nowadays JWT is very popular but this is the fundamental if our this flow is clear right all other authentication method we will get to know that how they are working why exactly we need those okay and if there is any question feel free to Ping in the comment section we'll discuss more see you soon bye
