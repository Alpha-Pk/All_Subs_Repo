=== Types of Role based Authorization (161s - 230s) ===

annotation based role based authorization there are two annotations which are primary used at the rate pre-authorize and add the rate post authorize. Pre-authorize means do the authorization before execution of the API logic. Before execution of the API do the authorization and post authorize means does authorization after execution of the API. So sometime what happen is you have a business logic. Let's say this is your endpoint user endpoint. So sometime is you need to execute the code logic and whatever the response you are getting inside that it might have certain data which we have to use and based on that we have to do the authorization logic authorization logic and then decide do we have to allow the user to access this API or not. Okay. So business logic runs first, authorization happens later in the authorized, post authorize and in pre-authorize authorization logic happen first and then only the API get executed. So first
