=== JWT Flow diagram (256s - 459s) ===

Clarity okay so this is you can say a flow of JWT today in the authentication uh part so this is let's say this is a client okay so this is the first happen is so this is your author authentication server so let's say any server which uh there are many third party libraries available which provides you the apis through which you can generate token validate token and all right so that's why any application authoriz authentication server so this is your app this is your application right so and this is let's say any third party application which support token generation and validation okay there are so many available today and that's one of the advantages of JW that you don't have to incorporate the authentication logic in your yourself there are already uh third party libraries available which we can use and they will take care of generating a token validating the token for you you just have to invoke them okay so let's call that third party application as authentication server so as a client right first I have to provide the username password so it will goes to an authentication server you provided your username password now what this authentication server will do it will generate the token and return right it will generate the token JWT and it will return back to the client now client will do is the client has the JWT now it wanted to access a particular resource right so what it will do is it will call our application resource where the resource is actually stored so it will call the resource and pass the JWT in the header in the header okay so let's say call the get API this is the get API let's say uh some details but it passed the JWT also now what this resource server your application will do is it will internally calls the API of authorization server let's say some verify API and it will pass the token which it get from the client now this all this logic present in to authentication server where it will validate yes this token is generated by me it is valid I'll tell you we will go more deeper into the token part how it validates it but just for now understand that author uh authentication server will validate the token and it will tell to your application that yes this is token is verified it's valid it's valid token once it is valid token resource owner will grant you the permission and it will turn you the data okay so this is the flow diagram of JWT so till now we are clear but before
