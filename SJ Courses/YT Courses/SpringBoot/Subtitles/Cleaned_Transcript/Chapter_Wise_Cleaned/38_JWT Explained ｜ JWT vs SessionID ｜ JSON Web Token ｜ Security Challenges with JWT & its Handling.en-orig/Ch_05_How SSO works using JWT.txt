=== How SSO works using JWT (1808s - 1924s) ===

let's say single sign on SSO so let's say that this is your company you have app one app 2 app three this is you so now I think you already know what how the single sign on works so first it will goes to certain authentication server where it provided username password it will get the JWT token now what does happen with the single sign on is let's say you are trying to log in to uh you are trying to opening this F1 www. let's say f1.com you trying to open it so what it will do is it will pass the JWT token also whatever the you have received and when this app1 received JWT token it internally try to verifies the your JWT if it verifies it will sign in right and all this user information like let's say username email ID is already available in your payload so it can help you to sign in say hi Shan how does it know it knows to JWT token itself that payload has may be my name my email ID ID so it able to verify me similarly when it able to goes to app2 it will pass the JWT app to uses the let's say verify this token from the server authentication server once it Val validated so verification signature everything is all good nothing tampered it will help me sign in also so here if you see only once I have provided the username password I got the token now the same token I'm using it to login at multiple places so that is known as SSO and this is implemented through JWT now there is
