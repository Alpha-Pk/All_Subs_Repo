=== JSessionID and its Problems, which JWT solved (459s - 794s) ===

we uh proceed further we need to understand what was popular before JWT and what are the problems with it so if you remember right before JWT there was something called session ID or many engineers might know it through J session ID I have worked with J session ID right so but this was very very popular session ID so in this what used to happen is let's say a client first what it will do is it login with username password the same let's say author authorization or authentication server third party server is there or generally what used to happen was resource server itself performs this task also generally there is no third party library but for simplification purpose let's say that you have another application which generate the session ID okay so first user provide a login with username password it goes to your application what it will do is it will create the session ID and save it in DB that's the important part it will put into the DB that this is the session ID right and store all the information like expiry time let's say session ID this belongs to this user this is the expiry time and other information that what are the roles like it can only uh fetch user details let's say certain roles it has put so all this data will put will be inserted into DB and this ID session ID which is just a unique random key will be given back to the client now with this ID so whenever it send a request in the cookies this client will also pass this J session ID right now this resource server has to call the DB now with this J session ID fetch all the details do all the logic okay expiry time does it expired roles these are the roles so whatever the operation it wanted me to do send like fetch this does it has this roles or not so it will do lot of validation at their side and if it is got validated then only it it will send the response else it will throw error so one of the major major catch is that every time you are hitting a request to F the details it is hitting the DB to face the details from this particular J session ID why because at the time of creation of J session ID when the time you provided username password it was stored in DB so it is State full and JWT is stat less there is no DB touch whatever the JWT token we are returning that JWT token has all the information like expiry regarding user information Etc so there is no need for d B and that's one of the advantage that it is stateless so your J session ID is stateful it rely on server side Estate Management right and since DB comes into the picture now you know that in case of distributed system this might cause a problem right let's say the server the application which creates this session ID into this DB right so there are let's say different cluster of DB this is Master Slave this is one cluster one let's say there are different cluster of DB so in a distributed system this all has to be sync up all DBS has to be sync up we don't know this get request this get request comes to where okay so in case of distributed system extra handling need to be taken care right and also one important part is during your get request your application has to query the DB perform validation on top of it so there would be an extra latency add up of quiring the DB now let's go back to the JWT again that what is it structure how does it become stateless right it needed so many information that roles expiry user ID and maybe some other unique ID how it is able to store this just a token right so this is the JWT structure
