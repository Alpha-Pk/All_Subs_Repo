=== Challenges with JWT and its Solution (1924s - 2967s) ===

one big problem with JWT token invalidation since JWT is stateless what does stateless means that these servers application Server doesn't know about this JWT at all right it is issued to a user now once it comes to them they just validate it if it is valid they will provide it the access now let's say that this user is fraud user this is fraud now I want to block now I want to block him block this user but this user has a token which has expiry time of let's say uh today is uh March let's say April till 20th or uh 21st April so till 21st April this has a valid token now even though this is a fraud user how you will make this token invalidate if I make this request to this application to it will check with the server pass this token hey this token is valid it its expiry is not yet come so how you will invalidate the Tok token so let's say that I have a blacklisted user how to invalidate a token before its expiration biggest problem so there are today multiple ways through which we are doing this first is server need to keep the list of blacklisted tokens right and either the DB or cash up lookup is required while validating so here you have authentication server right and then there you have app one app 2 multiple so it can maintain a token blacklisted blacklisted tokens right and each token is identified uning a particular ID I already told you there is something called J jti unique token ID right so now let's say that based on that it put the particular jti this token is invalid this is blacklisted now when uh this fraud user is passing the JWT token and it is trying to validate it it will check its blacklisted whether it's putting it in cash whether it is putting it in DB this blacklisted list it can validate that hey this token ID is there in the blacklisted L I need to deny it but this has a problem that which the problem earlier you were solving with session ID avoid look up of DB or cash now you are doing that okay so this is one problem but so this this will solve the problem but this again has a disadvantage that you have to do a DB or cash lookup okay what are the other ways the another way is just change the secret key itself so the change the secret key uh let's say that the uh earlier you were using for signing it you are using RSA let's say you are using private key public key one this is public key one now you have changed this key to 22 so now at the time of verification it will verify the signature right so now it will try to verify with the new public key so it will invalidate because it was signed through one private key one and if it will only be verified properly if you sign it with public key one only but now you have changed the key itself so it will it will Deni the user but what is the problem or if the apart from this fraud user if there are some genuine user also which was given a token using private key one which was given a token which was signed using private key one that will not be able to access access your data also they might have to log in again so that's the problem thatth second is if you change the secret key this will solve the problem but this will make the JWT invalidate for all genuine user also but this is you can say that again a problem what other thing we can do is make a token should be very short live instead of till April right let's say instead of till April what if I make that okay uh only for 5 minutes your token is valid only for 5 minutes your token is valid okay so this will resolve a problem but still till 5 minute that problem exist so this sometimes is very popular and also this is popular that token should be used only once so generally this is combined token should be used only once plus very short live let's say 5 minutes and it should be used only once after that you should generate a new token so this might solve a problem okay so but these are the different ways based upon your requirement uh one of the solution is implemented right so can you can also ask me that hey sh how do we know that to token should be used only once again to implement this this uh J J jti the unique token ID jti unique token ID is some somebody it has to be stored in cash or somewhere where it will check that hey I have seen this token before right so somewhere it has to do the DB so but you can say that the shortlived tokens are that's why very popular it's not whenever you get a token it's not valid for One Day 2 Day one month they are very shli use it for in within 5 10 minutes after that you have to might need to log in again okay so token invalidation has a problem genuine problem there are different solution for this based upon the requirement of the let's say the question which has been asked or your company multiple uh you can say that mix or one of the solution is implemented second JWT token is encoded not encrypted so it's less secure I told you right you have header then you have payload and then you have signature part ultimately token is your encoding of header dot encoding of payload dot encoding Bas 64 encoding of your signature value which ultimately got so here if you see that payload has mostly all the information and that is encoded and you know that this encoded payload you can simply do base 64 decode and you will get the data so this is less secure and for that jwe comes into the picture which is Json web encryption so what it does is nothing exactly same what we are doing but instead of encoding this uh letter of this payload it will do encryption of this payload that's all and at the receiver side when verify should be happen they can decrypt it so your authentication server knows the keys let's say RSA for encryption decryption encryption should happen through public key decryption should happen through private key we have already discussed in the previous video right but JWT is Right encrypt your payload only so JWT token is encoded not encrypted it's let secure what to do go for jwe in the jwe your payload part is encrypted and when your application calls the your application calls the server to verify it so server knows already the key how to decrypt it so they will decrypt the payload and then do it f the information and check expiry time and all and then do the validation okay so third the interview question comes is that in the JWT token ultimately let's say now you will get more clarity JWT and jws JWT token if it is not jws in the JWT token you have header you have payload and there is no signature so in the header you know that ALG part which say that what algorithm you are using for Signature it is none I am not signing it so such that is known as unsecured JWT where your algorithm is none for the signature that JWT should be rejected ultimately this is JWT there is no signature part and algorithm is none but this is known as unsecured JWT jws is Json WAP signature where you have a algorithm RSA hm whichever and you have doing the signature also putting the signature but that is known as JW s but since nobody is using JWT without the signature so that's why the terms are JWT jws used interchangeably because nobody use JWT token with algo none signature always present okay so without signature it is known as unsecured JWT which is never entertained it should never be it should always be rejected with algo with none remember now see this fourth and fifth part is very important from uh you can say that interview and uh uh the majorly attacker which use there is something called jwk jwk exploit so in the header I told you about type I told you about algo but there is something called jwk also right it is uh your key Json WAP key so public key is shared in this public key is shared in this all and it say that so you know that signature you have a message so message is sign using what key private key and this verify message is verified through public key now this message JWT you got a message and it is already signed right and now this user is trying to call a resource server and passing the JWT now this resource server should not have any logic to read the in the header there would be some jwk it will pass the public key also this n or E combinedly form a public key right this is I think your exponent part and this is your modulus part so these both combined make a public key so resource server should not verify your JWT with the public key which is coming inside this jwk no it might be possible that attacker has changed the message right and put its own public key and it changed the signature also the signature of the so it is possible that in the payload in the payload attacker has added some added some values and it signed it again with its attacker private key now it you got the JWT from this now you have to verify this sign digitally signed that it it is coming uh it is a valid or not is data is tampered or not if you use this public key which is coming in this jwk you might get yes it is verified because attacker might have added its public key hey verify with my public key so you might get accepted this payload that this is valid payload but might attacker might added some other details which is not intended so JWT exploit is that never use public key which is shared inside this jwk within a header so what to do how so now let's say that resource owner want a public key want a public key to verify it how to do that or let's say that resource owner is calling the authentication server now authentication server has to get the public key to sign it how it know there is something called K ID key ID I told you initially right there is something in the header also there is something called kid which you can add it inside a jwk also at the top also so this K ID is nothing but a key ID right and this key ID is used by your authentication server there is you whatever the authentication server you are using right they all maintain a well-known jwk Json so they have their own uh you can say that a link where they have list of public key list of public key which can be used so these are you can say that the wh listed publ key which authentication server can use and since these are public key everybody can know it right there's nothing to hide it so that's why they have generally this kind of we uh link whatever the author authorization server authentication server third party you are using they might have their own well-known JW k. Json this will contain the list of public key and each one will have a ID so this ID is present here so how the public key now authorization server knows that which public key I need to use to verify this signature it uses the K ID look it into its wh listed public key and choose when K ID okay this is the public key this is the K ID this is somewhere stored securely publicly and it will use that so now you know that these are the white listed these are the wh listed public keis which can be used right and with the kid you can get that with the kid you can uh use that from the list so you got it now if attacker you can say that what if attacker adds its own public key here right and pass that K ID right then it's an issue then we have to use a third party that's why we always say that choose a third party properly who is well known has taking all the precautions right security measures otherwise attacker may add its own public key into its Whit listed right somehow and then pass its own kid in the header and your author resource server uh or maybe you when you pass authentication server use that K ID and he said that yes this is present into my white listed public a let's accept this payload so you got it right the these are some of the issues with the uh JWT and this is actually what JWT is I hope it's uh clear what problem it solves what problem it has no there is no Silver Bullet for any technology JWT is highly used but it has certain gaps but there are like if used properly right then it's uh like very very beneficial it makes life easier authentication part okay guys see you soon bye and
