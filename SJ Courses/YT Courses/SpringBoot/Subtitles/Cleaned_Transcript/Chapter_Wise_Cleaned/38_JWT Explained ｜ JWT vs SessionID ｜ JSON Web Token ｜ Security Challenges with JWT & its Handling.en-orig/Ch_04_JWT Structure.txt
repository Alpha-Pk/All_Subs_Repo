=== JWT Structure (794s - 1808s) ===

so there are three things right JWT jws jwe e but whenever we are talking about JWT right ultimately actually we are always talking jws right so this you can say that always used together JWT jws we will I'll tell you how so let's say understand this JWT structure JWT structure has three part one after that there is a DOT period then this is the second part then again there is a DOT and this is the third part okay so this first part is known as header the second part is known as payload and the third part is known as signature right let's go one by one header so header already contains the metadata information of the token metad data like what is the type always JWT you will always find I have never seen any other value apart from JWT till now it's always JWT elgo the signing algorithm so here is the signing algorithm which you have used RSA or hmac right or if there are other different algorithms you can use it right RSA have already explained you in the previous cryptographic video and hmac is also I've explained you this is uh symmetric this is symmetric cryptography key this is a symmetric cryptography key right so I've already explained this so header contains information about metadata of the payload and the signature now your payload so I told you that payload actually contains the information or you can say that actually contain claims right so in simple terms user information or any additional information is kept here right JWT see that these are the claims the understand claims nothing like a just an information whatever you want to cap so this claims are divided into three parts registered claims public claims private claims okay so in the payload it is all comes under this only all comes under this only but let's say that there are certain this Words which are reserved by JWT so those are known as registered claims they have very specific meaning for example ISS which JWT understand as issuer the entity that issued the JWT for example ISS is issued by some uh JWT issuer at theate something some website link or something which identifies who is the issuer then subject identifies the user let's say subject sub 1 2 3 4 5 6 7 some unique identifier which identifies the user who is the user then your audience identify the recipient for which token is intended AUD let's say AUD would be uh your whatever your your company name or anything for which it identify the recipient then expiry time after this so exp is is like which tell the expiry time after this time token is not valid then NBF not before time before this token should not be accepted right so whatever the time is there before that you cannot accept the token and expiry is that after that this token is not valid then there is something called IAT the time which token is issued and jti is unique J ID some unique ID for this token so if you see that these names are reserved by JWT means they have pre uh predefined name and meaning but now let's say that apart from this registered name you have to put some other like I have put here email though those would be considered as a public claim it's a custom claim which can be shared and understood by multiple parties right so I have given the name email anybody can understand that it will contain an email ID of the user right or certains don't never ever put some very very you can say that confidential information inside a payload like some password or all never put never put your confidential information inside a payload right so let's say email apart from that let's say that uh okay um some C uh country so I can put country let's say India so if you see that these are not registered names I can put anything email country City or let's say that last name first name so these are you can say that a public claim which is understood which is understood by multiple parties right they know that okay country I will get the what is the country name for this user like City I know that this what city belongs to so these are the public claims these are custom claims you can use any but it should be shared and understood by multiple parties then there is something called private claims which is like understood by this your authentication server itself so let's say that it calls that username password now this authentication server has to generate the token now let's say it has put uh I some IM am right and this is uh let's say resource server and resource server one resource server 2 and it is passing this token to this JWT token to this now does resource server one understood this what is am what is am it hasn't understood it even though it is present inside here in the payload let's say another I am this is not understood by resource server one resource server two when they pass it to authorization server for validate only this understood it maybe for some internal purpose it might have added it which is not understood by multiple parties that's what known as private claim public claim is under by multiple parties like let's say country yes now resource server can understood that this is the country for the user but what is imem I don't know I don't know only authentication server might know it who has added it so that is known as private claim it is again a custom claim you can add anything but these are not identified for but these are intended for internal use only and not standardized right so these are not standard like not expected other parties to know this resource server one doesn't know this resource server two doesn't know this authentication server which created it they only noce it that's why known as private claim so understood so in the payload you can have have this claims but these claims are generally divided into three parts but it all under the same objects just an object now the third part is your signature this is very very important and that's why I told you sometime JWT and jws when we whoever tells you JWT sometimes also covers jws because signature the third part of uh token is your signature and that's what jws also is Json WAP signature okay so they are used you can say that interchangeably so now what signature now you now you have header now you have payload now what you will do how you will get a signature so what signature will do is it will first encode JWT header it will first encode header base 64 encoder it will do header payload also it will do uh encode basic 64 encoding for this payload so it will also do base 64 encoding for payload so it first encoded this header and it then it first encoded this payload okay then in con and concatenate the header and payload using this dot period so it will put a period and then it concact inate this so it's now one string okay so it would become like this your header and then your encoded payload encoded header encoded payload so this is known as your message or generally you can say that uh your information now you are using RSC or any cryptographic either a symmetric or symmetric to create a digital signature now you will pass it this message string to algorithm right so this is your one string message comma some key because now we are passing it to algorithm if you are using RSA for example a symmetric key you have to pass here private key if you are using hmac you have to pass one secret key and the same secret key would be used at the time of digitally signing it digitally verifying it right because in symmetric key the same key is used same key is used while signing and while verifying it while right and in the RSA two keys are used uh private key and public key so during signing it is signed through private key and during verifying the signature it is verified through public key okay now once you have uh got this signature let's say some string encode the signature also generate in the previous step you can encode it right now let's say you have so whatever the signature you got let's say x y z a b c this is the signature you got right you have to encode it also you have to encode it base 64 encode it also and concatenate using this dot so your header base 64 your header dot base 64 your payload dot now your base 64 encode of your signature got it so ultimately this is your JWT going to looks like so this is your P 64 of header dot this is your base 64 encoding of your payload dot this is your base 64 en code of your signature value and how the signature value is generated I told you okay so here I have shown one sample uh get request here I have shown you one sample API here if you see that I am making a get request to this some dummy API okay and in the header part there is a header called authorization you have to pass like beer and pass this this token now you can ask me right why we have to pass this beer so in the header right there are you can add multiple like key value right so one of the standard industry standard is that wherever you are passing the credential you have to pass the credential into authorization header so that it would be handled accordingly by the server right so that's why I told you right there are certain industry standard that yes I will get credential so it should be inside this header of the authorization so they can put a specific logic it should not be like randomly anywhere coming so this is the header name they have decided that okay authorization put inside this now inside this authorization there are multiple thing like basic beer when like you are doing that basic and then some encoded value a b c d e f g something so basic means that you are passing username password so when you decode this base 64 value decode this for the basic you will get like something that this username colon password but if you are passing like beer and then value then server will understand that okay this is not username password this is token beer means like industry standard means token is passed to me so I should behave in accordingly okay so you might have a different logic if user name password passed you might have a different logic that okay if beer comes means token is passed to me then I have to call certain server and validate it okay so JWT is always passed from client to server in the header of authorization but with the beerer word not basic remember basic is for username password so credentials always passed into authorization so this is the sample request okay so now before going to the challenges with JWT so now let's come back to Advantage first thing is you understand that it's a compact we are encoding it and because of its size it can be go inside a header itself HTTP header itself and it transmission is also fast because of its small size I have already told you it is stateless it is self-contained all the information you can Capt it expiry time user ID anything it is can be digitally signed so I've already told you jws generally what we understand JWT so we are we are adding a signature right so that is jws so means we are signing your token so JWT jws is using interchangeably you can say that so we can sign it using hm and RSA and when you sign it at the receiver end you can validate that yes the message is not changed at all right built in expiry time I told you that it is stateless you can add expiry uh time here so you don't need DB so built-in expir expiry mechanism is there custom claim additional data can be added in JWT like roles you have to add certain roles for this user you can add it into the claim itself so these are all the advantage of JWT which makes it stateless but now very very important part from interview perceptive what are the challenges with JWT so you understood till now right but before challenges I will also tell you that uh
