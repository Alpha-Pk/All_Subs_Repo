=== Introduction about JDBC (0s - 452s) ===

hey guys Shan side welcome to concept and coding and today we are going to see the spring boot jpa part one and let's start without wasting any time so here if you see that till now whatever we have done it's mostly a part of application logic now we have reached to a stage where we have to insert data into the database how we can do that so that's where the jpa jdbc comes into the picture Okay so before even jumping into the jpa we have to first understand the complete sequence right who who what all components actually get involved into this so here if you see that let's say that you are writing a spring boot application you have an application logic or you have written so many beans okay now what you do is if you have to use jpa what's the sequence what happen is you interact with an apis which is provided by jpa so always remember that jpa is mostly an interface right it provided a apis which you can interact with but it do not provide you the implementation implementation is provided by another component let's say in this case hibernate or if you don't want to use hibernate there is something like open jpa right or there I think Eclipse link and uh so these are different implementation of jpa okay so your application talks to jpa jpa internally has to be implemented by some other uh component which you decide which you want to use so generally hibernate is the most popular one so hibernate internally talks to or utilize jdbc API so again jdbc is again and interface so hiet uses its API it's interfaces and then there are specific DB drivers so this DB drivers is nothing but an implementation of this jdbc apis okay so for example you are using a MySQL DB okay you are using a MySQL DB then there would be a specific DB driver for it okay let's say it is connector J DB driver which is very very specific for MySQL which knows how to connect to mySQL and it provided the implementation of jdbc API similarly if you have something called uh postgress postgress SQL okay then it has its own DB driver it has its own DB driver okay so depending upon which DB you are using a specific that's why I written a specific DB driver is required okay this DB drivers is nothing but an implementation of jdbc apis this jdbc right uh Java database conect uh connectivity helps you to connect to a database okay through this API you can connect you can query you can fetch the data okay but remember jdb jdbc is nothing but an API interface actual work is done by drivers itself if you using MySQL then MySQL drivers know how to F the data okay then this jdbc now there is something called or framework is buildup object relational mapping framework so this is nothing but a bridge between Java object and your relational databases okay so here if you see that in the jdbc and specific DB driver everything we are in the form of SQL right we write queries and we try to connect uh through DB create DB or F the data insert everything is in SQL form but here with the OM framework where this jpa and let's say its implementation hiate we are using if this comes into the picture we don't have to your application don't have to directly work with the jdbc apis you don't have to write SQL queries to insert you can directly work with Java objects itself okay through Java objects itself you can maintain your relational databases so remember jpa jdbc is just for maintaining relational databases so before going to jpa Let's recall jdbc Java database connectivity okay so here if you see that it provide an interface to make connection with DB query DB and process the result whatever the data we get out of DB we also have to process it so it provide an API through which we can process it and implementation is provided by specific DB drivers if you're using my SQL DB then it has its specific drivers right so likewise so here if you see my SQL if you're using this DB what is the driver connector J and this is the class we have to load for it if you are using postgress SQL this is the driver and this is the class through which we can load this driver similarly S2 which is in memory relational database and its driver is this one S2 database engine and this is the class to load this driver so this driver now anybody say that what is the driver you know that driver is nothing but an implementation of jdbc apis so this expose jdbc API expose like okay get me connection uh insert let's say update delete but is it actually doing it no it's just an interface this drivers is actually has all the implementation how to get a connection how to insert it how to update it that from your application wise you don't have to worry about the spe specific driver code because you are dealing with the jdbc apis you do just get connection your application would do get connection okay you do update let's say an update and you provide it it's it's provided as the interface right now it's driver which is abstracted to us how it actually converted to something which is understand by relational database we don't have to worry about it so you don't worry like okay if I'm using my SQL driver my code will get changed if I'm using postgress SQL lot of code changes would be done no nothing like this you you already got it right why because we are dealing we are just invoking that get connection now this specific driver will take care of getting the connection so if you change from my SQL to posts your code will not change you are still calling get connection only internal implementation will change and that's okay so you can use anything
