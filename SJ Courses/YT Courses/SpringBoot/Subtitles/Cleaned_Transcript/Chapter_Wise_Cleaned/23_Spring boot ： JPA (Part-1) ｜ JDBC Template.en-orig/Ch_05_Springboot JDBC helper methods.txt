=== Springboot JDBC helper methods (2262s - 2788s) ===

boot has couple of methods very frequently used method I will tell you one is update and it takes two parameter one is SQL a string and another is where where AR variable arguments okay so this update method can be used for insert update and delete operation so like this insert into user so I have written my SQL query as an string and two Dynamic values which I need to insert okay so this is my string now I'm using jdbc template. update your query and second is variable arguments like variable arguments 1 2 3 could be anything right so two Dynamic right so one is your string second is your integer this one okay similarly it can be used for update so this is one method very very commonly used method of jdbc template another is this one for updating update the same your query and the second is prepared statement Setter PSS you can say it is also you can use for insert update delete it is just for like more granual level or you can say that more if your queries are very very complex then prepared statement Setter is can be useful the similar first thing is insert query like you can write your string okay uh you have to set Dynamic values now I'm instead of uh using the previous I'm now using prepared statement Setter jdbc template. update the first parameter is your string query now second is prepared statement Setter so here if you see uh jdbc template update string SQL so yeah this prepared statement Setter here if you see it is a functional interface okay so you can use Lambda expression and it has only one method which need to be implemented okay so one method which accept prepared statement so that's why I'm using Lambda expression in Java playlist I've already explained it in depth how you can use Lambda expression so here I am just providing the implementation for that get values itself that whatever the prepared statement set the string the first one is a string username so for the first index it's a string second Index this one second is integer right so I'm providing H so this is fun to this is our index 1 2 3 4 right so this is one this is two another question mark this would be three so this way you can add it okay so you can use it for insert update delete these are the two ways similarly now you have to fetch it right so one is query query method it takes one flavor is it takes two parameter one is a string which is your SQL query second is row mapper now it is used to F get multip mle rows you have to F multiple rows so now here if you see this query the jdbc template. query the first one is your select query a string and the second one is your row mapper so this row mapper is a functional interface which has one method map Pro which takes two parameters so that's why this two parameters and whenever this method get invoked this map Pro this is the implementation should run okay so see that what might be happening inside so in the user repository when I am doing get users let's see it might be doing query it going inside query uh here execute query after it executed the query it got a result set so result set can be like 10 rows 20 rows then it has to extract data out of it row mapper now it is here if you see it is iterating over this each row set each row and calling the map row okay so now this will get executed for each row Set uh there are total five rows then for each row I am creating a user object and do a mapping like in the row this column row. get in user ID so I'm putting into this user ID field it's just a mapping between a column and the object user object so row set. getet string is like username I putting into username like this and ultimately it will return a list it will just keep on adding into the result list and ultimately return this list of user the second flavor is query for list again first parameter is a St and second is class the purpose is to get single column of multiple rows single column of multiple rows so here jdbc template. query for list provide the query select username from users if there are total 10 users you will get username for all the 10 users only so one column whenever you want to fet the one specific column for multiple rows then go ahead use this query for list okay so the second parameter is class type so this username is a string docl class okay so here if you see query for list and we can search for here query for list what it does get single column row mapper so it is a single column it's uh mapping is based on single column like a single type a string then the third query could be like query for object the first is your SQL query then arguments and the required type get single row this is used for fetching a single row so jdbc template that query for object the first is your SQL query select estess from user where user ID equals to it could be dynamic so that's where your second parameter comes into the picture object array so I'm creating an object array only one right in the array so I'm putting like it should be replaced with one if there are more question mark you can just put into arrays it will sequentially it will update the question mark so it will change user ID equals to one with this and the third one is required type is the object in which this row has to be mapped to so I want to map to the user so internally it will do mapping use make use of getter and Setters of user and it will map this each column of this row into this user object and the last is like query for object SQL and required type get only single value it is used for get only single value like jdpc template get query for object select count asress from user you do integer do class so now here if you see it return only one single value so if you have any requirement where you get only one single value then you can use this get for object so these are generally different flavors which uh jdbc template provides which you can do to insert update delete or fetch the data okay so but with this I hope that at least this jdbc and this driver part should be properly clear where this exactly comes into the picture how you can connect to the DV what all problems are there in the plane gdbc what spring boot jdbc resolves it and how it resolve it because then only it makes sense to understand om framework and jpn hibernate okay so next we will start with the orm object relational mapping but we'll see you in the part two but if the part one if you have any doubts let's discuss in the comment section thank you bye okay
