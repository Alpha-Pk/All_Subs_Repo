=== Json Web Token (JWT) Validation (2712s - 3591s) ===

cake okay So now here in the token validation what we have to do understand that one now user is trying to access any API let's say users API this is a restricted API okay now it will pass JWT token to us JWT token now we have to validate this token validate token and if token is validated so this is your uh filter uh security framework If the token is validated, it has to pass it to the controller. Controller, it will fetch the resource and ultimately return. Okay. So that's what we have to do in inside a token validation. So here if user try to access any restricted resource, they need to pass the token into the authentication header like this. So whenever any request is coming let's say API users user has to pass like this in the authorization header they have to choose beer token and pass the token whatever we have written in the previous step. Okay. Now first thing first we have to write our filter. We have to write our filter which will do this validation. So I have created this filter again once per request filter I have extended it. Why? Because I need to make sure that this filter only runs once per request. So first thing I am doing no matter what request. So here if you see I'm not checking any users or anything. No matter what request comes I am extracting the JWT from the request. I am extracting the JWT. How I am extracting from the request? I am reading the header authorization and it starts with beer. So I'm take out the token out of it. So I have taken out the token. If token is not null, what I am doing is I am creating a new authentication object. Now you see that I am creating a custom authentication object. JWT authentication token. JWT authentication token. It's a child of abstract authentication token which is again a child of authentication object authentication. So here if you see that I am putting only one field token you can even have more but I have only one token. Okay. Okay. Currently authenticated I have set it to false. This is present inside this. So currently it's authenticated no and I have only one token. So whatever the token which I have get from the user. So what filter does is filter first thing is that create an authentication object. So I'm creating JWT authentication token and I have put this token whatever the u I'm getting from the user. So I have put here. So now I got a object but currently it's authenticated false authentication manager. I am calling authentication manager dot authenticate authentication token. Now at this point of time you must ask a question. Hey Shange the authentication manager if you from the previous generate token we have added only one provider which is DAO and DAO authentication provider do not understand this. So who will access it? Yes. So if you go to the security config we have in the provider apart from DAO we have added one more authentication provider JWT authentication provider. We have created a new provider which understand this authentication object. Okay. So we will see that. So we have created a new authentication provider. New authentication provider. So it's a child of authentication provider. It support is yes I understand JWT authentication token. So it understand this JWT authentication token. Okay. So authentication manager calls the authenticate object. Now the provider manager will iterate over the list of authentication provider. So there are two into the list. Right? So it will first go to DAO. DAO do you understand JWT authentication object? it will say no I don't understand then it will goes to this hey JWT authentication provider do you support this uh JWT authentication token it will say yes I support it and then it will call its authentication method so I'm calling it authenticate so what this provider authenticating it now you got this uh here right where it goes it has to validate the token it has to validate validate the token that's what I am doing it here now whatever the token I am getting whatever from this authentication I take out the token from this authentication I take out the token whatever the token I'm getting I created one more method JW inside of JWT util validate and extract username token so here I am creating JWT this method we have already created in generate and this is I have created newly red one So I am parsing the JWT token. I have put the signing key because it has since it's the HMAC right so it has to use the same key I am parsing it the claim the payload part I am taking out its body and from that body I taken out the subject and subject I have put it the username right and the subject I have put it the username so I'm taking out that extracting that info I'm taking out that username if username is null I am saying invalid JWT token Right. But if I get it, what I am doing it? I am calling user detail service. Load user by username. I am calling the DB fetching the user detail service. It has let's say username, password, role. Whatever all the information about the user, I got it. Here you can say that this one either you can enhance your uh JWT authentication token itself, right? or I am using username password authentication token where I'm putting this whole username details right and ultimately returning it because we have to do what we have to do is here if you see we have to pass it to the controller right so from this filter we have to we we don't have to return it from here itself we have to let the chain go and let it go through controller and controller also should have access to authentication object. Okay, it has access to authentication object. So that's what I am trying to do here. So once it is authenticated, token is valid. I am creating a au because I have to return an authentication object. Either you can extend this itself. This is also an authentication object. So currently I have only token here. Okay. So you can also create here uh JWT authentication token itself. Okay. And you can now change its authentication to true at this point of time authentication to true and also have a placeholder which is stored this user details which holds this user details. So for simplicity purpose I haven't extend all this information. So I uses the uh existing authentication object username password. I put the user details here. Okay, it will automatically make it to authenticated true. It will return it to my filter. Now if it is authenticated, I am storing into the security context holder. Now since it is stored in security context holder, even the controller have now access to it. Even the controller now have access to it. Now all I have to do is filter chain dodo filter. I don't have to stop the chaining. continue. After this filter, my validation is done. If authentication is true, I put it into the security context holder and go next. So maybe further more filters after that filters ultimately controller will get invoked and once controller done it will return the data what is asked for. Okay. So first thing we we have done is return an authentication filter. Okay. You understand that this is the second thing which I have a custom authentication object token. Okay, this is the third because uh since this is a custom authentication token, I have to write a provider which understand this. So I have a provider which supports this and I have written validate token. So this task is to just validate it. If it is uh validated true properly, I just load the user details and put it into the authentication object either the one which we have created. For now, I have used the existing one but we can use the what we have created. We just need to expand user details so that we can store it. After that uh it will go back to the filter but filter have store this authenticate object into the security context so that controller have access to it also if required and after that I'll let it go the chain so maybe further chaining after security filters ultimately control goes to controller so JWT util we know that we have added this method to validated and in the security config see that we already have this for authentication I have added it I have created an object of JWT validation filter this filter which we have created right so this is again a uh filter no add the rate component so we have to create an object of it so that's why I'm creating an object of it new filter and it required only authentication manager object so that's why I'm passing this authentication manager because it need only this okay and uh yes so that's what this one I already told you now this filter which we have created we have to put it into some space into the filter chain right so I am adding this filter after after this JWT authentication filter so this is my validation filter I am adding it after this JWT authentication filter so my first is let's say some security context holder filter by default. Second I have put is JWT authentication filter and after that it was username password authentication filter and now I am putting validation filter after this authentication filter JWT validation filter. So I'm specifying its place after this. Okay. And uh so I have created a filter and in the manager I have added that hey this is the provider I also need to add uh and what else yes for this provider you need to create an object because this is the authentication provider at the rate component is not there because this is the wall framework authentication provider child of this one right so I'm creating authentication provider object it required JWT user and user data service so I'm passing it so Everything is almost similar to what we have done previously in the generate token. So clear this part how validation is done and we have used the complete framework itself. So again I am registering a user. I got into the DV. Okay. SJ1 23 RO user. I am generating a token right passing a username password. What would happen is our authentication filter will catch DAO provider will use it to compare the username password. Ultimately filter will generate a token and return into the authorization header. Now I'm trying to access any restricted resource. I have to pass this token into this authorization beer token and pass this. Okay. Now what would happen is our filter get invoked. Filter get invoked. We have taken the token out of the header authorization. We have created a new authentication object pass to the pro uh authentication manager. Authentication manager will call to our provider. provider will validate it and uh first it will support yes and then it will pass it and then it will validate it and it will put into the uh security context and filter do filter after all the filters it will goes to our controller controller will yes fetch data successfully and return okay so controller logic is a business logic it has to fetch the user but yeah I just system outprint and something fetch user successfully. Okay. But got it. How I am doing the valid validate? If you pass try to pass the invalid token, I pass like hyphen invalid. This is not a valid token, right? I'm passing invalid. So here if you see it say forbidden authentication got failed. Okay. The control didn't reach till the controller. It itself will fail it. Okay. So this validate and extract username. Okay. So this will fail it will say bad credential exception. It will throw this exception. Invalid JWT token bad credential exception. Ultimately the request will fail. The it do filter will not chain ring will not happen. Controller till controller it will not
