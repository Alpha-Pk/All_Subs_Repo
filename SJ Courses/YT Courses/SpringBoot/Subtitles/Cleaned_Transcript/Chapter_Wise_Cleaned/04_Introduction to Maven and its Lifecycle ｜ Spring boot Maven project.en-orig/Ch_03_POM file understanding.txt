=== POM file understanding (367s - 978s) ===

downloaded right so this pom.xml we got right so the same pom.xml I have copied here and I will show you what exactly it is like how to read this pom.xml what generally the meaning of each properties and elements defined here okay so here if you see the first part this project okay this first part what here you will see certain kind of schema schema location okay so what is it it is specify the XML schema also make sure that our XML adhere to the correct structure and version defined by the May one so whatever the XML you can say that the rules it has provided in this schema this should be the name okay inside this parent you can have group ID artifact version only you can't by default use hey can I write this uh here something XY Z XY Z close can I do something no it is this XY Z is not defined in the schema of the parent inside this so we can't write something like randomly it has to follow properly what in within this element what all things can we write right so it should follow certain schema here if you see the parent part this one this is used to define the parent project the project if you remember which we have downloaded from Spring boot right so the Palm which we got so in the Palm you will see this parent block means this is the child P child PR of what it's a child form of this so in May one each p. XML is a child of some other form right if it is if you have defined this So currently it's a child of this this project form so whatever this project so these are you know that group ID and artifact ID uniquely identify a particular project in the mavan central okay so in this project this is the version there would be some pom.xml so that would be parent of this p parent okay so each P has one parent if there is no parent defined in the Palm let's say that if there is no parent defined in the pal then Mayan automatically by default make inherited from a super P so in Mayan every P which you are writing right internally by default it's a child of super pom.xml okay so generally in our case in our PM the p. XML which got download loed in our project this one which got downloaded with the project it is given that it's a child of this this project so spring boot starter parent spring boot some starter parent project it's pom.xml so this is its parent and this itself might be a child of super pom.xml okay so you got it so this there could be some other uh there could be some other parent for this one or if no then It ultimately would be a child of this so we can in simple terms we can say that super p is something which is at the top of the hierarchy now whatever the pal you will create whether this or this generally it's a child of this super pal only okay so the parent used to define the parent project current project inherit the configuration from a specified Pro uh parent project so our project in is inherited the configurations from here so in in our pm. XML if you see this is very small P right there is not much configuration why because it might be using the configuration from its parent in its parent it might have provided lot of configurations right so since it's a child of it it will use its configuration whatever its dependencies all the dependency which this parent has or the configuration which parent has all will automatically comes to your child also if this parent field is not a specified Maven by default inherit the configuration from Super form so this is the link of the maven super pom I have provided okay so you can check out the uh super p. HTML here there are lot of configurations by default configurations are available which generally comes to your project this configuration and dependencies okay so you understood what is the meaning of this parent means our project is generally a child of this project actually this three parts I have already informed you right in the previous uh when we created this springboard project group ID artifact ID version these three uniquely identify our project which we are creating so this is the company name generally you can say this is your ultimately the project name and this is the version currently so this uniquely identified your project in the mavan central okay then we have something called properties so what is properties is it's a key value pair for configuration right so this is a key this is a value okay so it can be referenced throughout the XML file so now in all other XML file I can use this property this key how let's say in this dependency group ID artifact ID and let's say I want to use certain version I can use like that that let's say version I can do like this dollar this java. version Java do version close so now this dollar java. version ultimately what MAV will do it will check in the properties hey for this key is there any value defined yes 17 is defined so this will get ultimately with replaced with 1710 okay so anything which you define in properties it's a generally a key value and then this key can be referenced anywhere in the in this form okay okay now let's see the next property which is like repositories so what does it do this is from Mayan look for depend project dependency and download the artifact jars so here if you see that in this this repository we have defined it one URL okay so what this URL is this is the like URL from where you want to download your dependencies so this is the next if you see we have a list of dependencies first is this this is our first dependency then this is our second dependency now from where this dependency will get resolved from this okay so this is where this is from where MAV look for project dependency and download the jars right so this is generally you can say that I have written it for you but here if you see in the project which is downloaded you will find that this is missing this is not there this is not there generally this is added uh this is a by default which Mayan added you can say that maybe in the super pom.xml you might find that that this is by default value is present if you don't add it from its parent P this will get added so by default it will always download from mavan Central only by default so even if you don't write this maybe if you go and check in super pom you will find this so that's why it is not required to add it here and even in the pom.xml which you downloaded if you double check you won't find it because I have added just to show you that from where it downloaded the dependency so till till dependency is all clear right now actual May one starts when you see the build this build element and before I tell you you about this build you first need to understand the build life cycle then only you would be able to understand why we have to provide this build what's the usage uh Deep dive into this but first let's understand the build life cycle So Below is the maven build life
