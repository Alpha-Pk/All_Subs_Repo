=== Demo of Topic Creation (630s - 854s) ===

Let's see that so here &gt;&gt; [clears throat] &gt;&gt; So I have in the configuration class order events this is the topic name which I have to create partition I am creating for replica two and all other information I have kept the same but let's set up the cluster one and let me first remove everything like first remove everything like broker one dot logs gone broker two dot logs gone. one dot logs gone controller two dot logs gone so all four you can say that our servers in a cluster is gone now what we have to do is that first create a cluster ID. So, this is our new cluster ID. And now we will create controller and broker and attach it with this cluster. &gt;&gt; So, just copied it. And just replacing this new cluster ID. First I'm creating controller one. Then controller two with the same cluster ID. So, now in a one cluster, I have now two controller and two broker. Okay? Now, let's just start the controller one first. So, yeah, controller one. And here I will start the controller two. Okay? So, both controller started. Both controller started? Yes. Now, let me start the broker. We'll start broker two. So, both broker are also started. Let's see the status for order {hyphen} events topic. Currently this event this topic does not exist at all. Now, let me start the server. So, here if you see, I have this bean only bean at the rate configuration to create an order {hyphen} events. So now let's start the server. Server is started. Now let's again check describe topic order hyphen events. Partition 0 1 2 3. So total four partition and two replicas. That's what we have defined here. Four partition, two replicas. So we have set up we have created the topic through a Spring Boot itself. Okay? And all the library which we required is this. Because this internally brings the Kafka admin which is used to manage the topic. Now once the topic is created, we have to send the event. Now there is a work of Kafka template.
