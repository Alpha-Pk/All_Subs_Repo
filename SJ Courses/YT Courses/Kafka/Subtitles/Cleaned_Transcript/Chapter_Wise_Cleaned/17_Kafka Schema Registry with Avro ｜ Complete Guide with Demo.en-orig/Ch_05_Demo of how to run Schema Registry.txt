=== Demo of how to run Schema Registry (982s - 1114s) ===

Let's do that first. So, here, this I have downloaded Confluent 8.2.0, and within this ETC folder, schema registry, we have a schema-registry.properties. So, here in this one, we have, let's say, VI ETC schema registry. So, here, I have already set up like uh the port number 8085, which I wanted, and the Kafka bootstrap server. Okay? And no need to change the topic name, which it will create. Okay? So, you can add this. Now, let's uh start. So, before starting the schema registry, we have to start the controller. So, I've already showed you how you can start the controller. So, I'm starting controller one. Starting controller two. Okay, so both are started. I'm started broker one. I'm starting broker two. Okay, and after that now I'm starting. We have to start the schema. So here, we can run this command. So let's copy this one. So I'm already inside this 8.2.0. Okay, so I'll use this from here. Yeah, so I'm already inside this Confluent 8.2.0 folder. So we are starting this. Okay, and we can run this command to see running. So it is showing empty means everything runs. So our first thing is running. Our first setup schema registry setup is done. So our Kafka is up. Both the broker and controller and schema registry is up. Cool.
