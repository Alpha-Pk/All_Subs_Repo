=== Kafka Cluster (1243s - 1354s) ===

cluster is nothing but a group of brokers working together to provide scalability, fall tolerant and high availability. Very very important. Scalability means distribute load across multiple servers. Fall tolerance means continue operate even if broker fails. High availability means no single point of failure. Kafka do not have a single point of failure. So in this example now see KL clavka cluster is has it has multiple broker. So one broker what does one broker has is one broker can have some topics and of some topic it can have some of their partition. Okay and Kafka custer we have multiple such brokers. We have multiple search brokers which has c some topics and of those topics some partitions. [snorts] Now everything need to be coordinated. Okay. It takes care of certain requests. It take care of certain requests. It takes care of certain request. That's why scalability distributed load across multiple server. These are all servers. Fault tolerance. Even if one broker fails, even if one partition fails, even if one topic fails, the Kafka is up and running. No single point of failure at any point of time. Whether broker fails, whether partition fails, whether topic fails, it's up and running. How? I will show you. Okay. Now the question is how? How this multi-broker architecture where we have multiple broker able to achieve this three things very important part the leader follower
