=== OneToOne Implementation (380s - 810s) ===

right so as we are looking into one to one mapping let's first implement this scenario as well right so let's go back over here inside our intell ID and as you can see over here we already have applicant right now we need to add one more entity which is resume right so let's add it first so what I'll do I'll just add a new Java class let's name it as resume and I will just create this class let me just mark it as a entity as this guy will be our entity I will just add a couple of fields over here first will be ID right so usually first will be ID let me just add a long ID over here let me just add a long ID over here I will mark it as at theate ID we add one more annotation that is generated value so that it is auto incremented right and I will add a strategy over here and let's say strategy is Generation type. Auto right so what is strategy and everything we have already covered as a part of last video you might be aware of it already right so resum we have let's say some content right so content or let's say the resume is saved inside some file storage and we are giving that file storage link or something over here right now as we have seen over here that that resume have applicant ID which is a foreign key of your applicant right so what we will do we will add a field over here in resume that is basically applicant right so what I will do I will say private and I will say applicant and applicant right so this is basically the applicant resume will be Associated to right so I will convert this guy also to private encapsulation right so applicant we have added over here but we need to Define that this is basically a foreign key that means the ID inside applicant this particular ID will be foreign key for our resume class right so in order to do that what we need to do is we can make use of join column annotation right so join column right the join column is basically a notation that can be used in order to add the foreign key inside your class right so here I'm adding a foreign key for this particular object right so we need to add some kind of key name over here right what we'll do we'll add let's say applicant ID right so applicant ID is basically the foreign key for your applicant table right so if you see over here we have added this applicant ID over here right so what we can do we can add it in camel case as well right so applicant ID right so this applicant ID will be saved inside a database when we actually Sav the applicant and we can add one more field that is nullable nullable means this column cannot be null when you add cant inside your resume right so that is basically not null constraint that we have in SQL right so similar stuff over here right so that is basically join column so here when we actually look the table this field will be replaced by applicant ID right which will be stored inside your database right and whenever we F or save the data it will be automatically converted to app entity so it is kind of a join between these two column right so jpa will form this query automatically so we don't need to create all these queries spring data will create everything for us right and now about the relationship we need to define the relationship over here right so we can make use of annotation one to one right so one to one right so one to one means one applicant is associated with one resume like that right this annotation will actually tell jpa that this particular entity have one to one mapping with your other entity right and jpa will use this annotation to set up similar kind of relationship while it creates the query or creates the table right so that is basically easily you can map these two tables by using one to one mapping right right so in order to save some time I have added some code over here so first of all I have added these Getters and Setters over here in the entity in a resume entity right after that I'm creating a controller in order to add the resume right so here I'm creating a post mapping where I'm taking application ID and along with that I'm taking a request body resume as well for this particular applicant ID our resume will be added right and after that I have created this particular service request mapping where I have added one function which will first find the applicant by using applicant ID that we are getting and after that we will just add that particular applicant ID to resume and save the resume right if it is not found then we will will just return this error that applicant is not found with that particular ID right and that's it and apart from that I have added this resume repository which is kind of a jpa reposit right simple dimple stuff right now what we can do let me just rerun this application over here let's see what happens and let me bring up my Postman right so first of all what we are going to do is we are going to add one applicant right so we have this mangalam Shu over here so what we will do we will just send and this should add one applicant right as usual stuff now what we'll do we'll just create another API so this is the API that I have added so here what I'm doing / API SL one that is one is basically the applicant ID of the applicant that we have added right that we are having resume which is basically our this particular end point over here applicant id/ resume right now here what we are doing we are just adding a Content this is basically a dummy string I'm giving over here now if I send this then we are getting a response we are getting id1 we are getting this content and the respective applicant is populated over here right now what we can do over here is let me just go over here and let's log into database right so how to log into database we have seen it multiple times isn't it so we'll just go and log into our H2 console over here password should be password let's try to connect right oh we have logged in now if you go ahead and see resume then you will see that select star from resume we have ID one we have content and applicant ID is one right so that is what we were talking about when we talked about one to one mapping it will just store the ID of your applicant over here right so here only ID will be stored inside a database when we fetch it the applicant also will be pulled from your database by using this applicant ID and it will be mapped to this particular entity resum and when we get the response on the console the entire applicant instead of just getting the ID right that is basically the mapping that is basically the relation between your applicant and resume right here what we are doing over here is let me go back over here and if you see here we are adding resume applicant ID inside your resume right so it is basically one directional mapping one to one mapping unidirectional mapping right so we are adding resume for respective applicant what we can do we can add resume while we are adding applicant itself so here only we can add
