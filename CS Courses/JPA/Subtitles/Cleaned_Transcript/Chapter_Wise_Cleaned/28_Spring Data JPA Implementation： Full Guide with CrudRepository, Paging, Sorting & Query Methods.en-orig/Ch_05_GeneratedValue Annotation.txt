=== GeneratedValue Annotation (375s - 527s) ===

right now whenever you create a new applicant whenever you create a new applicant are you going to manage this ID on your own is user going to manage it do user care about this ID right this is something which is handled by our system right user do not care about that if I'm applying for job why do I care about the ID that is going to saved in the systems database right so I'm not going to care so our system should handle the autogeneration of this particular ID right so that is when at the rate generated value comes into picture right so this is going to be a generated value right now generated value needs a strategy right what is a strategy let's come back to it so there are different type of strategies basically so I'm selecting Auto over here so this generated value takes one argument that is basically a strategy and this generation type is basically enum which pass that particular value so here if you see here we have multiple generation types right so we have table we have sequence identity uu ID Auto right so we are making use of Auto now right now each persistence provider have its own way of creating this primary key right for example hibernate will have have its own way so basically if you choose Auto then then your persistence provider or let's say hibernate in this case should pick up any one of these right you just select auto and let the other guy do whatever it wants to do right the uid is basically a universally unique identifier right so that will be used for your primary key in this case identity means it should use identity column in order to provide the ID sequence means it should use database sequence in order to generate the primary key and table means your persistence provider must assign a primary key for that particular entity using a underl database table right it says primary key for entity using an underling database table to ensure uniqueness right so that is basically another generation type so this is basically the generation type right so let's come back over here so for now what I'm doing I'm selecting Auto over here now this is a minimum thing that you need in order to use entities right there are many other things that you can use but these are the minimum things right now what we are going to do let me just create few getter sets quickly so what I will do I'll go to generate and I will create Getters and Setters for everyone right so that will do that for me so all the gets and sets are generated right so that is basically our entity so we have successfully created The Entity for us right so that is
